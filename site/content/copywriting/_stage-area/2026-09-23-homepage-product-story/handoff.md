# Homepage product story — implementation handoff

Routes: homepage and booking. Homepage-only shell variant is additive; other routes and console consumers retain defaults.

The hero explains team, repository, platform, and cloud relationships, plays once, and holds. Existing cloud, delivery, and agent stories remain independent. Hero and workflow rendering share palette, icons, path sampling, and clock behavior; the hero is not a resource DAG. Source data and geometry stay separate. Reduced motion and no JS keep the complete story readable.

Real product capture supplied by the founder: complete light-mode app export retained intact, with annotations outside the image and a full-size link. It shows the self-hosted Planton instance on GKE that the founder confirms is used to manage Planton SaaS. Frame copy identifies it as an actual export. Annotations describe only the visible boundaries, workloads, and resource details. No screenshot pixels, resource names, or product controls are fabricated.

Book a Demo leads for engineering buyers. Start Free and coding-agent setup remain explicit secondary paths. Form fields, endpoint, and confirmed-booking semantics are unchanged. Analytics contain only closed event vocabulary and placement/category metadata.

Verify: full build, existing workflow suite, revised homepage/booking suite, desktop/mobile captures, reduced motion/no JS, keyboard, replay after completion, mocked failures and bookings, and before/after local performance evidence. No conversion uplift or field Core Web Vitals is claimed from lab checks. Preview before the previously authorized PR/merge and verify deployment after.

Visual refinement: the hero uses three aligned inputs around the central Planton mark without a repeated wordmark. Result handoffs reverse the same authored routes. The full architecture export replaces the cropped initial capture; no CSS cropping on mobile, and a visible full-size link supports close inspection.
