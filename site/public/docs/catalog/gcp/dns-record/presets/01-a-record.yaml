apiVersion: gcp.openmcf.org/v1
kind: GcpDnsRecord
metadata:
  name: my-a-record
spec:
  projectId:
    value: "<gcp-project-id>"
  managedZone:
    value: "<dns-zone-name>"
  type: A
  name: "<subdomain.example.com.>"
  values:
    - <ipv4-address>
  ttlSeconds: 300  # default from spec; 5 minutes
