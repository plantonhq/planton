apiVersion: scaleway.openmcf.org/v1
kind: ScalewayPrivateNetwork
metadata:
  name: my-network
spec:
  vpcId:
    value: "<your-vpc-id>"
  region: fr-par
  ipv4Subnet: 10.0.1.0/24
  enableDefaultRoutePropagation: false  # recommended_default from spec
