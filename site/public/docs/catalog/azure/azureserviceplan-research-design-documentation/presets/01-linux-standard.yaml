apiVersion: azure.openmcf.org/v1
kind: AzureServicePlan
metadata:
  name: my-service-plan
spec:
  region: <azure-region>
  resourceGroup:
    value: "<your-resource-group-name>"
  name: <plan-name>
  osType: Linux  # default from spec
  skuName: S1
  workerCount: 1
