apiVersion: openstack.openmcf.org/v1
kind: OpenStackLoadBalancerPool
metadata:
  name: my-round-robin-pool
spec:
  listenerId:
    value: <listener-id>
  protocol: HTTP
  lbMethod: ROUND_ROBIN
  description: Round-robin HTTP pool
