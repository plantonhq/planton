apiVersion: aws.openmcf.org/v1
kind: AwsIamUser
metadata:
  name: my-read-only-user
spec:
  region: <aws-region>
  userName: <service-user-name>
  managedPolicyArns:
    - arn:aws:iam::aws:policy/ReadOnlyAccess
  disableAccessKeys: true
