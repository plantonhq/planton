apiVersion: kubernetes.openmcf.org/v1
kind: KubernetesService
metadata:
  name: my-service
spec:
  namespace: "<your-namespace>"
  name: my-service
  type: cluster_ip
  selector:
    app: "<your-app-label>"
  ports:
    - name: http
      port: 80
      targetPort: "8080"
      protocol: TCP
