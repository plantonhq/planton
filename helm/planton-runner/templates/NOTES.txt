The Planton runner is installed as {{ include "planton-runner.fullname" . }}
in namespace {{ .Release.Namespace }}.

On first boot the runner joins the control plane with its token, registers
itself as "{{ include "planton-runner.runnerName" . }}", and receives its own
identity -- it appears in your organization's Runners list the moment it joins.

Check that it is up and connected:

  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
  kubectl logs -n {{ .Release.Namespace }} deploy/{{ include "planton-runner.fullname" . }}

{{- if .Values.build.enabled }}

Build capability is ENABLED. One step remains to make this cluster a build
cluster: register it as a build connection (console: Connections -> Build,
or `planton apply` a TektonConnection naming this runner), then verify it --
the readiness check probes this exact cluster placement, from this runner,
over the same queue real builds ride. Its run-watch check confirms the runner
may watch its namespace's PipelineRuns and TaskRuns, which is all live build
status needs: no Tekton CloudEvents sink has to be configured.

Builds land in namespace {{ include "planton-runner.buildNamespace" . }}.
{{- else }}

Build capability is DISABLED (build.enabled=false). This runner serves
infrastructure operations only. To also run build pipelines on this cluster,
install Tekton Pipelines, then upgrade with:

  --set build.enabled=true
{{- end }}
