---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: plantonplatforms.planton.ai
spec:
  group: planton.ai
  names:
    kind: PlantonPlatform
    listKind: PlantonPlatformList
    plural: plantonplatforms
    singular: plantonplatform
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Deployment lifecycle phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Deployed platform version
      jsonPath: .status.version
      name: Version
      type: string
    - description: Web console URL once ingress is admitted
      jsonPath: .status.consoleUrl
      name: URL
      type: string
    - description: Whether the public internet reaches the front door, as the operator
        concluded
      jsonPath: .status.reachability
      name: Reachability
      type: string
    - description: License delivery mode (Community when none configured)
      jsonPath: .status.license
      name: License
      type: string
    - description: Email provider as declared (NotConfigured when spec.email is absent)
      jsonPath: .status.email
      name: Email
      type: string
    - description: GitHub hosts as declared, with (App) where an install App is registered
        (NotConfigured when spec.github is absent)
      jsonPath: .status.github
      name: GitHub
      priority: 1
      type: string
    - description: 'Whether the platform''s database is being saved: Healthy, Deploying,
        Failing, Unavailable, or NotConfigured'
      jsonPath: .status.backup.state
      name: Backup
      type: string
    - description: Why the platform is in its phase, in plain language
      jsonPath: .status.conditions[?(@.type=="Ready")].message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          PlantonPlatform is the Schema for deploying a complete Planton platform
          on any Kubernetes cluster. A single PlantonPlatform resource orchestrates
          the full stack: data layer, supporting services, and application layer.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PlantonPlatformSpec defines the desired state of a self-hosted Planton deployment.
              A minimal spec requires only the version field; all other fields have sensible defaults.
            properties:
              bootstrap:
                description: |-
                  bootstrap seeds the first-boot workspace: a default organization with a
                  starter environment, and the admins who own it. The first declared admin
                  to sign in lands inside a ready workspace as its owner and the install's
                  platform operator -- no create-org ceremony. Every field is optional;
                  defaults produce an organization and environment both named "default",
                  with spec.identity.adminEmail as the sole admin.
                properties:
                  admins:
                    description: |-
                      admins are the emails granted organization ownership AND the install's
                      platform-operator role -- at boot when their account already exists, or
                      the moment they first sign in. Editing this list takes effect on the
                      next control-plane restart (declarative, never fire-once). Defaults to
                      [spec.identity.adminEmail] when that field is set.
                    items:
                      type: string
                    type: array
                  environment:
                    description: environment is the starter environment seeded inside
                      the organization.
                    properties:
                      name:
                        description: name is the environment's display name. Defaults
                          to the slug.
                        type: string
                      slug:
                        default: default
                        description: slug is the environment's URL-safe identifier
                          within the organization.
                        type: string
                    type: object
                  iacProvisioner:
                    default: tofu
                    description: |-
                      iacProvisioner is the IaC engine seeded as the organization's deploy
                      default. "tofu" (OpenTofu, the default) and "terraform" both execute
                      with the bundled OpenTofu binary against the Terraform-compatible
                      module catalog. Seeded create-once: a different choice made later in
                      the console is never overwritten by a restart. Pulumi is deliberately
                      not accepted here -- it has no zero-config state backend (its file
                      backend requires a state-encryption passphrase), so seeding it as the
                      default would produce an install where every deploy fails.
                    enum:
                    - tofu
                    - terraform
                    type: string
                  organization:
                    description: organization is the default organization every declared
                      admin owns.
                    properties:
                      name:
                        description: name is the organization's display name. Defaults
                          to the slug.
                        type: string
                      slug:
                        default: default
                        description: slug is the organization's URL-safe identifier
                          (also its id).
                        type: string
                    type: object
                  secretBackend:
                    description: |-
                      secretBackend declares the organization's default secret backend,
                      seeded create-once at boot (a choice made later in the console is never
                      overwritten). Both supported kinds are credential-free by construction:
                      "platform" stores secrets in the bundled vault (deployed by default;
                      incompatible with spec.vault.enabled: false) and is seeded
                      automatically when the vault runs and nothing is declared here;
                      "awsSecretsManager" stores secrets in AWS Secrets Manager, reached with
                      the control plane pod's own cloud identity (workload identity via
                      spec.controlPlane.serviceAccountAnnotations, instance profiles, or env
                      credentials) -- no key is ever typed or stored. When neither applies,
                      no default backend exists and the console guides secret-related
                      features until one is created.
                    properties:
                      awsSecretsManager:
                        description: awsSecretsManager configures the awsSecretsManager
                          type.
                        properties:
                          region:
                            description: region is the AWS region secrets are stored
                              in (e.g. "ap-south-1").
                            type: string
                        required:
                        - region
                        type: object
                      type:
                        description: |-
                          type selects the backend kind: "platform" (the bundled vault) or
                          "awsSecretsManager" (ambient-authenticated AWS Secrets Manager).
                        enum:
                        - platform
                        - awsSecretsManager
                        type: string
                    required:
                    - type
                    type: object
                    x-kubernetes-validations:
                    - message: awsSecretsManager.region is required when type is awsSecretsManager
                      rule: self.type != 'awsSecretsManager' || (has(self.awsSecretsManager)
                        && size(self.awsSecretsManager.region) > 0)
                type: object
              build:
                description: |-
                  build configures the pipeline-build capability: Tekton on this cluster,
                  the runner's build worker and webhook, and the seeded build routing.
                  Enabled by default -- builds power Service Hub, and an install without
                  them is half a product; every field is optional.
                properties:
                  enabled:
                    default: true
                    description: |-
                      enabled controls whether the build capability is deployed. Default
                      true: builds power Service Hub -- an install without them can deploy
                      infrastructure but never build a service from source, which is half
                      the product. Opting OUT is the deliberate act (e.g. an install that
                      only manages infrastructure). Builds also require the runner: with
                      spec.runner disabled, builds follow it off.
                    type: boolean
                type: object
              components:
                description: |-
                  components toggles the optional platform capabilities that are disabled by
                  default to keep the minimal deployment footprint small. The policy engine
                  is not among them: every platform runs it, like its database.
                properties:
                  graph:
                    description: |-
                      graph configures Neo4j for relationship graph queries.
                      Disabled by default.
                    properties:
                      enabled:
                        default: false
                        description: enabled controls whether Neo4j is deployed.
                        type: boolean
                      storageClassName:
                        description: |-
                          storageClassName pins the Neo4j data volume to a StorageClass.
                          Defaults to spec.storage.storageClassName, then the cluster default.
                        type: string
                      storageSize:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          storageSize is the persistent volume size for Neo4j data. Defaults to
                          spec.storage.size, then 10Gi.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                type: object
              console:
                description: |-
                  console configures the Planton web console (Next.js).
                  The console is always deployed. Image defaults to the official
                  GHCR image tagged with spec.version.
                properties:
                  externalConfigSecretName:
                    description: |-
                      externalConfigSecretName references a user-created Secret in the same
                      namespace containing external configuration (Auth0, NextAuth, etc.).
                      All keys in the Secret are injected as environment variables via envFrom.
                    type: string
                  image:
                    description: image overrides the default container image for the
                      web console.
                    properties:
                      repository:
                        description: |-
                          repository is the full container image repository
                          (e.g., "ghcr.io/plantonhq/planton/control-plane").
                        type: string
                      tag:
                        description: tag overrides the image tag. Defaults to spec.version
                          when empty.
                        type: string
                    type: object
                  replicas:
                    default: 1
                    description: replicas sets the number of web console pods.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              controlPlane:
                description: |-
                  controlPlane configures the Planton control plane monolith.
                  The control plane is always deployed. Image defaults to the official
                  GHCR image tagged with spec.version.
                properties:
                  externalConfigSecretName:
                    description: |-
                      externalConfigSecretName references a user-created Secret in the same
                      namespace containing external service credentials (Auth0, Stripe,
                      GitHub, etc.). All keys in the Secret are injected as environment
                      variables via envFrom. The control plane will fail to start without
                      this configuration when running with the real (non-placeholder) image.
                    type: string
                  iacModulesVersion:
                    description: |-
                      iacModulesVersion overrides the release the platform downloads official
                      IaC module artifacts from (both engines: the OpenTofu module zips and
                      the Pulumi module binaries ride the same release tag). Unset -- the
                      shape every install should have -- the platform resolves modules at its
                      own catalog release: the same pin its schemas and chart bundle come
                      from, so a kind the platform accepts always has its module published.
                      Set it only to route around a retracted artifact set; it selects among
                      published releases and controls nothing else -- deliberately NOT the
                      platform image version (spec.version) and NOT the chart bundle. Every
                      value must have a published artifact set under
                      downloads.planton.dev/releases/<version>/ or deploys fail at module
                      download.
                    pattern: ^v\d+\.\d+\.\d+$
                    type: string
                  image:
                    description: image overrides the default container image for the
                      control plane.
                    properties:
                      repository:
                        description: |-
                          repository is the full container image repository
                          (e.g., "ghcr.io/plantonhq/planton/control-plane").
                        type: string
                      tag:
                        description: tag overrides the image tag. Defaults to spec.version
                          when empty.
                        type: string
                    type: object
                  replicas:
                    default: 1
                    description: replicas sets the number of control plane pods.
                    format: int32
                    minimum: 1
                    type: integer
                  serviceAccountAnnotations:
                    additionalProperties:
                      type: string
                    description: |-
                      serviceAccountAnnotations are applied to the control plane pod's
                      dedicated Kubernetes ServiceAccount. This is the workload-identity seam
                      for the control plane's OWN cloud calls -- ambient-authenticated secret
                      backends and their KMS encryption keys (e.g.
                      eks.amazonaws.com/role-arn: <role> on EKS, the GKE/AKS equivalents
                      elsewhere). The ServiceAccount always exists, so adding annotations
                      later is a pure spec edit -- no pod surgery. Distinct from
                      spec.runner.serviceAccountAnnotations, which grants the DEPLOY worker
                      its cloud identity; this grants the platform itself one.
                    type: object
                type: object
              database:
                description: |-
                  database configures storage for the data-layer components
                  (PostgreSQL and Redis).
                properties:
                  postgresql:
                    description: postgresql configures the platform's PostgreSQL cluster.
                    properties:
                      backup:
                        description: |-
                          backup declares where the platform's own database is backed up and
                          how (see PostgreSQLBackupSpec). Absent means no backup: the database
                          lives on one volume in this cluster and nothing copies it anywhere.
                          The Backup column of `kubectl get plantonplatform` says which.
                        properties:
                          objectStore:
                            description: |-
                              objectStore is the bucket the backups go to and how the database's pods
                              authenticate to it.
                            properties:
                              azureBlob:
                                description: 'AzureBlobObjectStoreSpec: Azure Blob
                                  Storage.'
                                properties:
                                  credentialsSecretName:
                                    description: |-
                                      credentialsSecretName names a Secret in the platform's namespace with
                                      key AZURE_STORAGE_CONNECTION_STRING. Empty means Azure AD workload
                                      identity on the database's ServiceAccount -- the preferred posture; see
                                      serviceAccountAnnotations.
                                    type: string
                                  storageAccount:
                                    description: |-
                                      storageAccount is the account the container belongs to. Identifies the
                                      endpoint for the keyless posture.
                                    minLength: 1
                                    type: string
                                required:
                                - storageAccount
                                type: object
                              destinationPath:
                                description: |-
                                  destinationPath is the bucket and prefix: s3://bucket/path (s3 and
                                  r2), gs://bucket/path (gcs), or the container URL (azureBlob).
                                minLength: 1
                                type: string
                              gcs:
                                description: 'GCSObjectStoreSpec: Google Cloud Storage.'
                                properties:
                                  credentialsSecretName:
                                    description: |-
                                      credentialsSecretName names a Secret in the platform's namespace with
                                      key APPLICATION_CREDENTIALS holding a service-account JSON key. Empty
                                      means GKE Workload Identity on the database's ServiceAccount -- the
                                      preferred posture; see serviceAccountAnnotations.
                                    type: string
                                type: object
                              r2:
                                description: |-
                                  R2ObjectStoreSpec: Cloudflare R2 through its S3 API. R2 has no keyless
                                  posture; the key pair is derived from a Cloudflare API token carrying an
                                  R2 permission group (access key id = the token's id; secret access key =
                                  the lowercase hex SHA-256 of the token's value) and always arrives as a
                                  Secret. The endpoint follows from the account and the bucket's
                                  jurisdiction, and the only region R2 accepts is set for you.
                                properties:
                                  accountId:
                                    description: accountId is the Cloudflare account
                                      that owns the bucket.
                                    minLength: 1
                                    type: string
                                  credentialsSecretName:
                                    description: |-
                                      credentialsSecretName names a Secret in the platform's namespace with
                                      keys ACCESS_KEY_ID and SECRET_ACCESS_KEY.
                                    minLength: 1
                                    type: string
                                  jurisdiction:
                                    description: |-
                                      jurisdiction is the bucket's data residency: empty or "default" for
                                      standard storage, else "eu", "fedramp", or "us". A jurisdictional
                                      bucket is served ONLY through its own host, so this must match how the
                                      bucket was created.
                                    enum:
                                    - ""
                                    - default
                                    - eu
                                    - fedramp
                                    - us
                                    type: string
                                required:
                                - accountId
                                - credentialsSecretName
                                type: object
                              s3:
                                description: 'S3ObjectStoreSpec: Amazon S3, or any
                                  S3-compatible store by endpoint.'
                                properties:
                                  credentialsSecretName:
                                    description: |-
                                      credentialsSecretName names a Secret in the platform's namespace with
                                      keys ACCESS_KEY_ID and SECRET_ACCESS_KEY. Empty means the database's
                                      pods inherit an IAM role (IRSA, EKS Pod Identity, an instance profile)
                                      -- the preferred posture; see serviceAccountAnnotations.
                                    type: string
                                  endpointCASecretRef:
                                    description: |-
                                      endpointCASecretRef names the CA bundle a private endpoint's TLS chains
                                      to, when the system trust store does not carry it.
                                    properties:
                                      key:
                                        description: key within the Secret.
                                        minLength: 1
                                        type: string
                                      name:
                                        description: name of the Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  endpointURL:
                                    description: |-
                                      endpointURL points at an S3-compatible store (MinIO, Ceph, a NAS).
                                      Empty means Amazon S3.
                                    type: string
                                  region:
                                    description: |-
                                      region of the bucket. Required by Amazon S3; some compatible stores
                                      ignore it.
                                    type: string
                                type: object
                            required:
                            - destinationPath
                            type: object
                            x-kubernetes-validations:
                            - message: 'an object store names exactly one backend:
                                s3, gcs, azureBlob, or r2'
                              rule: '[has(self.s3), has(self.gcs), has(self.azureBlob),
                                has(self.r2)].filter(x, x).size() == 1'
                            - message: an s3 or r2 store's destinationPath starts
                                with s3://
                              rule: '!(has(self.s3) || has(self.r2)) || self.destinationPath.startsWith(''s3://'')'
                            - message: a gcs store's destinationPath starts with gs://
                              rule: '!has(self.gcs) || self.destinationPath.startsWith(''gs://'')'
                            - message: an azureBlob store's destinationPath is the
                                container URL, https://<account>.blob.core.windows.net/<container>/<path>
                              rule: '!has(self.azureBlob) || self.destinationPath.startsWith(''https://'')'
                          retentionPolicy:
                            default: 30d
                            description: |-
                              retentionPolicy is how long the store keeps backups and the WAL that
                              goes with them: an integer and a unit, d (days), w (weeks), or m
                              (months). Default 30d.
                            pattern: ^[1-9][0-9]*[dwm]$
                            type: string
                          schedule:
                            default: 0 0 2 * * *
                            description: |-
                              schedule is when base backups run, as a SIX-field cron with seconds
                              first (the database operator's grammar): "0 0 2 * * *" is daily at
                              02:00 UTC, the default. The first base backup always runs the moment
                              backups are declared, whatever this says.
                            type: string
                          serviceAccountAnnotations:
                            additionalProperties:
                              type: string
                            description: |-
                              serviceAccountAnnotations go on the ServiceAccount the database operator
                              creates for the database's own pods (it names that account after the
                              PostgreSQL cluster: "{platform}-postgres"). This is the keyless backup
                              identity: EKS "eks.amazonaws.com/role-arn", GKE
                              "iam.gke.io/gcp-service-account", AKS "azure.workload.identity/client-id"
                              -- the cloud-side binding is written against that account, and no
                              credential exists in the cluster. On GCS the bound identity needs BOTH
                              roles/storage.objectAdmin and roles/storage.legacyBucketReader: the
                              plugin verifies the destination with a bucket-level read before every
                              archive, and object administration alone does not carry it.
                            type: object
                        required:
                        - objectStore
                        type: object
                      recoverFrom:
                        description: |-
                          recoverFrom restores this platform's database from another platform's
                          archive instead of creating it empty (see PostgreSQLRecoverFromSpec).
                          Honored only when the database is first created.
                        properties:
                          objectStore:
                            description: |-
                              objectStore is the store the source platform archived to. Recovery only
                              reads from it; this platform's own backups go to spec.database.postgresql.backup.
                            properties:
                              azureBlob:
                                description: 'AzureBlobObjectStoreSpec: Azure Blob
                                  Storage.'
                                properties:
                                  credentialsSecretName:
                                    description: |-
                                      credentialsSecretName names a Secret in the platform's namespace with
                                      key AZURE_STORAGE_CONNECTION_STRING. Empty means Azure AD workload
                                      identity on the database's ServiceAccount -- the preferred posture; see
                                      serviceAccountAnnotations.
                                    type: string
                                  storageAccount:
                                    description: |-
                                      storageAccount is the account the container belongs to. Identifies the
                                      endpoint for the keyless posture.
                                    minLength: 1
                                    type: string
                                required:
                                - storageAccount
                                type: object
                              destinationPath:
                                description: |-
                                  destinationPath is the bucket and prefix: s3://bucket/path (s3 and
                                  r2), gs://bucket/path (gcs), or the container URL (azureBlob).
                                minLength: 1
                                type: string
                              gcs:
                                description: 'GCSObjectStoreSpec: Google Cloud Storage.'
                                properties:
                                  credentialsSecretName:
                                    description: |-
                                      credentialsSecretName names a Secret in the platform's namespace with
                                      key APPLICATION_CREDENTIALS holding a service-account JSON key. Empty
                                      means GKE Workload Identity on the database's ServiceAccount -- the
                                      preferred posture; see serviceAccountAnnotations.
                                    type: string
                                type: object
                              r2:
                                description: |-
                                  R2ObjectStoreSpec: Cloudflare R2 through its S3 API. R2 has no keyless
                                  posture; the key pair is derived from a Cloudflare API token carrying an
                                  R2 permission group (access key id = the token's id; secret access key =
                                  the lowercase hex SHA-256 of the token's value) and always arrives as a
                                  Secret. The endpoint follows from the account and the bucket's
                                  jurisdiction, and the only region R2 accepts is set for you.
                                properties:
                                  accountId:
                                    description: accountId is the Cloudflare account
                                      that owns the bucket.
                                    minLength: 1
                                    type: string
                                  credentialsSecretName:
                                    description: |-
                                      credentialsSecretName names a Secret in the platform's namespace with
                                      keys ACCESS_KEY_ID and SECRET_ACCESS_KEY.
                                    minLength: 1
                                    type: string
                                  jurisdiction:
                                    description: |-
                                      jurisdiction is the bucket's data residency: empty or "default" for
                                      standard storage, else "eu", "fedramp", or "us". A jurisdictional
                                      bucket is served ONLY through its own host, so this must match how the
                                      bucket was created.
                                    enum:
                                    - ""
                                    - default
                                    - eu
                                    - fedramp
                                    - us
                                    type: string
                                required:
                                - accountId
                                - credentialsSecretName
                                type: object
                              s3:
                                description: 'S3ObjectStoreSpec: Amazon S3, or any
                                  S3-compatible store by endpoint.'
                                properties:
                                  credentialsSecretName:
                                    description: |-
                                      credentialsSecretName names a Secret in the platform's namespace with
                                      keys ACCESS_KEY_ID and SECRET_ACCESS_KEY. Empty means the database's
                                      pods inherit an IAM role (IRSA, EKS Pod Identity, an instance profile)
                                      -- the preferred posture; see serviceAccountAnnotations.
                                    type: string
                                  endpointCASecretRef:
                                    description: |-
                                      endpointCASecretRef names the CA bundle a private endpoint's TLS chains
                                      to, when the system trust store does not carry it.
                                    properties:
                                      key:
                                        description: key within the Secret.
                                        minLength: 1
                                        type: string
                                      name:
                                        description: name of the Secret.
                                        minLength: 1
                                        type: string
                                    required:
                                    - key
                                    - name
                                    type: object
                                  endpointURL:
                                    description: |-
                                      endpointURL points at an S3-compatible store (MinIO, Ceph, a NAS).
                                      Empty means Amazon S3.
                                    type: string
                                  region:
                                    description: |-
                                      region of the bucket. Required by Amazon S3; some compatible stores
                                      ignore it.
                                    type: string
                                type: object
                            required:
                            - destinationPath
                            type: object
                            x-kubernetes-validations:
                            - message: 'an object store names exactly one backend:
                                s3, gcs, azureBlob, or r2'
                              rule: '[has(self.s3), has(self.gcs), has(self.azureBlob),
                                has(self.r2)].filter(x, x).size() == 1'
                            - message: an s3 or r2 store's destinationPath starts
                                with s3://
                              rule: '!(has(self.s3) || has(self.r2)) || self.destinationPath.startsWith(''s3://'')'
                            - message: a gcs store's destinationPath starts with gs://
                              rule: '!has(self.gcs) || self.destinationPath.startsWith(''gs://'')'
                            - message: an azureBlob store's destinationPath is the
                                container URL, https://<account>.blob.core.windows.net/<container>/<path>
                              rule: '!has(self.azureBlob) || self.destinationPath.startsWith(''https://'')'
                          serverName:
                            description: |-
                              serverName is the name the source's archive is filed under -- printed
                              on the source platform's status (status.backup.serverName), or read from
                              the bucket's own listing when the source is gone. Every platform archives
                              under its own name, so a restored platform never writes over its source.
                            minLength: 1
                            type: string
                          targetTime:
                            description: |-
                              targetTime recovers to a point in time (RFC 3339). Empty recovers to
                              the end of the archive.
                            type: string
                        required:
                        - objectStore
                        - serverName
                        type: object
                      replicas:
                        default: 1
                        description: |-
                          replicas is the number of PostgreSQL instances in the cluster: a
                          primary plus streaming-replication hot standbys with automated
                          failover. 1 (the default) is a single instance; raising it after
                          install grows the topology live.
                        format: int32
                        minimum: 1
                        type: integer
                      storageClassName:
                        description: |-
                          storageClassName pins the PostgreSQL volumes to a StorageClass.
                          Defaults to spec.storage.storageClassName, then the cluster default.
                        type: string
                      storageSize:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          storageSize is the persistent volume size per PostgreSQL instance.
                          Defaults to spec.storage.size, then 10Gi. (Defaults resolve in the
                          operator, not in this schema, so the platform-wide size can tell
                          "unset" apart from "chosen".) Sizes can grow after install by editing
                          this field; they can never shrink.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                  redis:
                    description: |-
                      redis configures storage for the redis-protocol cache instance. The
                      field keeps the protocol-role name every consumer speaks; the engine
                      serving it is Valkey (see the valkey resources for the rationale).
                    properties:
                      maxMemory:
                        description: |-
                          maxMemory is the dataset ceiling as a Valkey size ("768mb", "1gb").
                          Defaults to 768mb. Always below the container's memory limit: hitting
                          the limit is an OOM kill, hitting this is an eviction.
                        pattern: ^[0-9]+(b|kb|mb|gb|k|m|g)?$
                        type: string
                      maxMemoryPolicy:
                        description: |-
                          maxMemoryPolicy is what happens at maxMemory. Defaults to allkeys-lru:
                          a finished run's log and a stale cache entry fall out first, the stream
                          being tailed is the hot set. noeviction fails writes instead (right for
                          a durable store, wrong for this one).
                        enum:
                        - noeviction
                        - allkeys-lru
                        - volatile-lru
                        - allkeys-lfu
                        - volatile-lfu
                        - allkeys-random
                        - volatile-random
                        - volatile-ttl
                        type: string
                      persistence:
                        description: |-
                          persistence keeps the dataset on a volume and replays it after a pod
                          restart (append-only file). Defaults to true: the store holds the live
                          build-log stream, and a restart mid-build must not blank the log a
                          person is tailing. What is persisted always fits, because maxMemory
                          bounds it under the container's memory limit -- a persisted store with
                          no ceiling reloads more than it may hold after a memory kill and never
                          comes back. Set false for a pure in-memory cache that starts empty.
                        type: boolean
                      resources:
                        description: |-
                          resources sizes the store's container. Defaults to requests of 100m CPU
                          and 256Mi memory with a 1Gi memory limit and no CPU limit. Size the
                          memory limit above maxMemory: Valkey needs headroom for its
                          append-only-file rewrite and client buffers.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      storageClassName:
                        description: |-
                          storageClassName pins the store's volume to a StorageClass. Defaults to
                          spec.storage.storageClassName, then the cluster default. Read only
                          while persistence is on.
                        type: string
                      storageSize:
                        anyOf:
                        - type: integer
                        - type: string
                        description: |-
                          storageSize is the persistent volume size for the store. Defaults to
                          spec.storage.size, then 1Gi. Read only while persistence is on.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                type: object
              email:
                description: |-
                  email declares the mail provider this install sends through -- an SMTP
                  relay or a Resend account, the address it sends as, credentials by
                  Secret reference. Its presence is what turns email on: invitations are
                  emailed as well as linked, alerts reach people, and the sign-in page
                  offers "Forgot password?". Without it the install sends nothing and
                  says so wherever an email would have gone.
                properties:
                  from:
                    description: |-
                      from is the identity every email carries: the address the install
                      sends as and the display name beside it. The relay must permit sending
                      as this address (a mailbox's own address, or one it has Send As rights
                      to); SPF and DKIM for the domain are the domain owner's job.
                    properties:
                      address:
                        description: |-
                          address the install sends as, e.g. no-reply@planton.acme.com. Required
                          whenever email is declared: there is no default address, because a
                          default would name somebody else's domain.
                        minLength: 1
                        type: string
                      name:
                        default: Planton
                        description: name shown beside the address in mail clients.
                        type: string
                    required:
                    - address
                    type: object
                  replyTo:
                    description: |-
                      replyTo is where a person's reply lands -- a help desk or a shared
                      mailbox -- when the sending address is a no-reply one. Unset, replies
                      go to from.address.
                    type: string
                  resend:
                    description: resend sends through Resend's API with an API key.
                    properties:
                      apiKeySecretRef:
                        description: |-
                          apiKeySecretRef points at the Resend API key. Required by reference:
                          the key is never inline. Reaches the control plane as a mounted file,
                          so a rotated key is live on the next send with no restart.
                        properties:
                          key:
                            description: key within the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    required:
                    - apiKeySecretRef
                    type: object
                  smtp:
                    description: |-
                      smtp sends through any SMTP relay: a workplace mail system (Exchange
                      Online, Google Workspace, an internal smart host) or a transactional
                      vendor's SMTP endpoint (SES, SendGrid, Postmark, Mailgun, Resend).
                    properties:
                      caBundleSecretRef:
                        description: |-
                          caBundleSecretRef points at a PEM CA bundle for verifying the relay's
                          TLS certificate -- the private-CA case, the classic enterprise blocker.
                          Omit it when the relay's certificate chains to a public root.
                        properties:
                          key:
                            description: key within the Secret.
                            minLength: 1
                            type: string
                          name:
                            description: name of the Secret.
                            minLength: 1
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      credentialsSecretName:
                        description: |-
                          credentialsSecretName names a kubernetes.io/basic-auth Secret in this
                          namespace whose username and password keys sign in to the relay:

                            kubectl -n <namespace> create secret generic planton-email \
                              --type=kubernetes.io/basic-auth \
                              --from-literal=username=... --from-literal=password=...

                          Omit it for a relay that admits this cluster by network address. The
                          values are never inline and reach the control plane as mounted files,
                          so a rotated password is live on the next send with no restart.
                        type: string
                      host:
                        description: host of the relay, e.g. smtp.office365.com or
                          smtp-relay.corp.acme.com.
                        minLength: 1
                        type: string
                      oauth2:
                        description: |-
                          oauth2 signs in with a token from an OAuth2 client-credentials grant
                          (SASL XOAUTH2) instead of a password. Exchange Online: tokenUrl
                          https://login.microsoftonline.com/<tenant>/oauth2/v2.0/token, scope
                          https://outlook.office365.com/.default, the app registration's client
                          id and secret, and user = the mailbox the app may send as.
                        properties:
                          clientId:
                            description: clientId of the app registration.
                            minLength: 1
                            type: string
                          clientSecretRef:
                            description: |-
                              clientSecretRef points at the app registration's client secret.
                              Required by reference: the secret is never inline.
                            properties:
                              key:
                                description: key within the Secret.
                                minLength: 1
                                type: string
                              name:
                                description: name of the Secret.
                                minLength: 1
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          scope:
                            description: scope requested for the token.
                            minLength: 1
                            type: string
                          tokenUrl:
                            description: tokenUrl is the provider's OAuth2 token endpoint.
                            pattern: ^https://
                            type: string
                          user:
                            description: |-
                              user is the mailbox the token sends as -- the account the app
                              registration has been permitted to use.
                            minLength: 1
                            type: string
                        required:
                        - clientId
                        - clientSecretRef
                        - scope
                        - tokenUrl
                        - user
                        type: object
                      port:
                        default: 587
                        description: |-
                          port the relay listens on. 587 is the submission port most relays use
                          with STARTTLS; implicit-TLS relays (security: tls) usually listen on
                          465; an internal plaintext relay on 25.
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      security:
                        default: starttls
                        description: |-
                          security protects the connection: starttls (required upgrade after
                          connect, the default), tls (implicit TLS from the first byte), or none
                          (plaintext, credential-free relays only).
                        enum:
                        - starttls
                        - tls
                        - none
                        type: string
                    required:
                    - host
                    type: object
                    x-kubernetes-validations:
                    - message: 'smtp authenticates one way: set credentialsSecretName
                        for a username and password, or oauth2 for a token, not both'
                      rule: '!(has(self.credentialsSecretName) && size(self.credentialsSecretName)
                        > 0 && has(self.oauth2))'
                    - message: 'security: none would send credentials in the clear;
                        keep security at starttls or tls, or drop credentialsSecretName
                        and oauth2 for a relay that admits this cluster''s address
                        without them'
                      rule: '!has(self.security) || self.security != ''none'' || (!(has(self.credentialsSecretName)
                        && size(self.credentialsSecretName) > 0) && !has(self.oauth2))'
                required:
                - from
                type: object
                x-kubernetes-validations:
                - message: 'email declares exactly one provider: set spec.email.smtp
                    for a relay or spec.email.resend for a Resend account, never both,
                    never neither'
                  rule: has(self.smtp) != has(self.resend)
              gateway:
                description: |-
                  gateway tunes the built-in front door that serves the platform when
                  ingress is disabled: one in-cluster proxy Service presenting the console,
                  the API, and sign-in on a single origin, reached with one kubectl
                  port-forward command (printed in the gateway component's status). Every
                  field is optional; the gateway deploys automatically whenever ingress is
                  off and retires when ingress takes over as the front door.
                properties:
                  image:
                    description: image overrides the default nginx container image
                      the gateway runs.
                    properties:
                      repository:
                        description: |-
                          repository is the full container image repository
                          (e.g., "ghcr.io/plantonhq/planton/control-plane").
                        type: string
                      tag:
                        description: tag overrides the image tag. Defaults to spec.version
                          when empty.
                        type: string
                    type: object
                  localPort:
                    default: 8080
                    description: |-
                      localPort is the localhost port the port-forward is expected to serve
                      the platform at. Change it only when the default clashes with something
                      already listening on your workstation.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              github:
                description: |-
                  github declares the GitHub hosts this install works with, the GitHub
                  App registered for the whole install on each (so every organization
                  connects in one click), and whether each host can deliver webhooks to
                  the install. Absent, the install offers github.com with "bring your own
                  App" and judges webhooks by the front door -- the right posture for an
                  adopter on github.com who has declared nothing.
                properties:
                  hostLogin:
                    description: |-
                      hostLogin lets connections use a GitHub sign-in the control plane's own
                      process carries (a GITHUB_TOKEN in its environment). Off by default on
                      a shared install: the token would act for everyone. The desktop's local
                      instance turns it on because the machine's own sign-in is the person's.
                    type: boolean
                  hosts:
                    description: |-
                      hosts lists the GitHub hosts this install works with, in the order the
                      connection wizard offers them. Declare github.com to say it explicitly
                      or to give it an App; declare a GitHub Enterprise Server to make it the
                      first choice. A host that is not declared can still be typed by hand
                      in a connection, without an install App.
                    items:
                      description: GithubHostSpec is one GitHub host and what the
                        install offers on it.
                      properties:
                        app:
                          description: |-
                            app is a GitHub App registered on this host for the whole install.
                            With it, every organization connects in one click and gets webhooks
                            and check runs; without it, teams bring their own App. The App must be
                            registered on THIS host: an App on github.com cannot sign for an
                            enterprise server.
                          properties:
                            clientId:
                              description: |-
                                clientId is the App's client ID from its settings page -- the JWT
                                issuer when the control plane mints installation tokens. Not a secret.
                              minLength: 1
                              type: string
                            privateKeySecretRef:
                              description: |-
                                privateKeySecretRef names the Secret key holding the App's private key
                                as GitHub generated it: the PEM text, unencoded.
                              properties:
                                key:
                                  description: key within the Secret.
                                  minLength: 1
                                  type: string
                                name:
                                  description: name of the Secret.
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                            webhookSecretRef:
                              description: |-
                                webhookSecretRef names the Secret key holding the webhook secret set
                                on the App, so deliveries are verified as the App's own. Without it
                                deliveries from this host are accepted unverified -- acceptable only
                                on a host that cannot deliver at all.
                              properties:
                                key:
                                  description: key within the Secret.
                                  minLength: 1
                                  type: string
                                name:
                                  description: name of the Secret.
                                  minLength: 1
                                  type: string
                              required:
                              - key
                              - name
                              type: object
                          required:
                          - clientId
                          - privateKeySecretRef
                          type: object
                        host:
                          description: |-
                            host is the hostname as people type it in a browser: github.com, or a
                            GitHub Enterprise Server such as github.example.com. A hostname, never
                            a URL -- the control plane derives the API address (api.github.com for
                            github.com, host/api/v3 for an enterprise server).
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)+$
                          type: string
                        webhooks:
                          default: auto
                          description: |-
                            webhooks is whether this host can deliver webhooks to the install.
                            auto (the default) judges by the front door: reachable when the door
                            is on the public internet. Declare reachable for a GitHub Enterprise
                            Server that shares the install's private network -- it can deliver
                            even when the internet cannot -- and unreachable when a public host
                            cannot reach a door that is public only inside a corporate perimeter.
                            Where webhooks cannot arrive, Planton polls GitHub for pushes and every
                            team is told so.
                          enum:
                          - auto
                          - reachable
                          - unreachable
                          type: string
                      required:
                      - host
                      type: object
                    maxItems: 8
                    type: array
                    x-kubernetes-list-map-keys:
                    - host
                    x-kubernetes-list-type: map
                type: object
              identity:
                description: |-
                  identity tunes the bundled identity server (Keycloak) that provides
                  browser sign-in. Identity is not a toggle: every install deploys it, so
                  "deploy Planton" always yields working multi-user sign-in -- through the
                  port-forward front door without ingress, or at the public hostname with
                  it. Every field is optional: a zero-config install gets a fully
                  provisioned identity server, and the console's first-run setup page
                  creates the first admin (adminEmail remains the GitOps override that
                  seeds one instead).
                properties:
                  adminEmail:
                    description: |-
                      adminEmail is the GitOps override for the first admin: when set, a user
                      with this email is seeded into the realm, their generated one-time
                      password lands in the {crName}-identity-admin-user Secret, and first
                      sign-in forces a password change and asks for their name. When unset
                      (the default), NO user is seeded (there is deliberately no generic
                      pre-baked admin identity): the first person to open the console is
                      asked for THEIR email plus a setup code from the
                      {crName}-identity-setup-code Secret -- proof of cluster access -- and
                      the admin account is created from that, with admin privileges pinned to
                      the created account rather than to an email string.
                    type: string
                  image:
                    description: image overrides the default Keycloak container image.
                    properties:
                      repository:
                        description: |-
                          repository is the full container image repository
                          (e.g., "ghcr.io/plantonhq/planton/control-plane").
                        type: string
                      tag:
                        description: tag overrides the image tag. Defaults to spec.version
                          when empty.
                        type: string
                    type: object
                  realm:
                    default: planton
                    description: |-
                      realm is the Keycloak realm name that holds Planton's users and the
                      console's OIDC client.
                    type: string
                type: object
              ingress:
                description: |-
                  ingress configures external access to the Planton web console and API.
                  When disabled, the platform is served by the built-in front-door gateway
                  over kubectl port-forward (see spec.gateway) -- sign-in included.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: |-
                      annotations are added to the Ingress resource for controller-specific
                      behavior. They take precedence over the operator's own defaults.
                      Ignored with gatewayRef (the Gateway API expresses behavior in typed
                      fields, not annotations).
                    type: object
                  enabled:
                    default: false
                    description: |-
                      enabled controls whether a front-door route is created.
                      When false, use kubectl port-forward for access.
                    type: boolean
                  gatewayRef:
                    description: |-
                      gatewayRef serves Planton through a Gateway API Gateway the cluster
                      already runs (Istio, Envoy Gateway, Cilium, a cloud Gateway) instead
                      of an Ingress controller: the operator attaches an HTTPRoute for the
                      hostname to that Gateway. The Gateway is the cluster team's object and
                      is never modified -- its listeners decide which hostnames are admitted,
                      which namespaces may attach routes, and how HTTPS is terminated; the
                      operator reads those facts and explains any mismatch in this
                      component's status.
                    properties:
                      name:
                        description: name of the Gateway.
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          namespace of the Gateway. Defaults to the platform's own namespace.
                          A Gateway in another namespace must allow routes from this one
                          (spec.listeners[].allowedRoutes.namespaces).
                        type: string
                      sectionName:
                        description: |-
                          sectionName pins the route to one named listener of the Gateway. When
                          empty, the route attaches to every listener whose hostname admits the
                          platform's hostname.
                        type: string
                    required:
                    - name
                    type: object
                  hostname:
                    description: |-
                      hostname is the domain Planton is served at (e.g., "planton.example.com").
                      When empty, the operator derives a hostname from the front door's
                      published address (an IP becomes a sslip.io magic-DNS name; a load
                      balancer DNS name is used directly) and reports it in status.consoleUrl.
                      The derived hostname is a zero-setup convenience for evaluation, not a
                      production endpoint.
                    type: string
                  ingressClassName:
                    description: |-
                      ingressClassName selects which ingress controller serves Planton.
                      When empty (and gatewayRef is unset), the cluster's default
                      IngressClass is used.
                    type: string
                  reachability:
                    default: auto
                    description: |-
                      reachability declares whether the public internet can reach this
                      front door. The operator cannot observe that from inside the cluster,
                      and the capabilities that need an inbound path from the internet --
                      keyless cloud connections, GitHub webhook delivery -- are offered only
                      where the door is public. "auto" (default) resolves from the door's
                      shape: a hostname served over HTTPS is public, anything else private.
                      Declare "private" for an HTTPS door only your network can reach
                      (split DNS, a corporate CA); declare "public" to affirm it. Only
                      "public" is refused on a disabled ingress -- a port-forward door is
                      never reached from the internet -- while "private" there is simply
                      true. A definition older than this field refuses the declaration
                      outright (server-side apply never prunes an unknown field), and the
                      refusal names the operator to upgrade.
                    enum:
                    - auto
                    - public
                    - private
                    type: string
                  tls:
                    description: |-
                      tls enables HTTPS. When absent, Planton is served over plain HTTP and
                      the ingress component's status notes the connection is unencrypted --
                      except with gatewayRef, where the route attaches to whichever listener
                      matches the hostname and HTTPS is inferred from that listener.
                    properties:
                      issuer:
                        description: |-
                          issuer references a cert-manager Issuer or ClusterIssuer that obtains
                          and renews the certificate automatically. Requires cert-manager to be
                          installed in the cluster.
                        properties:
                          kind:
                            default: Issuer
                            description: kind of the issuer resource.
                            enum:
                            - Issuer
                            - ClusterIssuer
                            type: string
                          name:
                            description: name of the Issuer or ClusterIssuer.
                            minLength: 1
                            type: string
                        required:
                        - name
                        type: object
                      secretName:
                        description: |-
                          secretName references an existing kubernetes.io/tls Secret in the same
                          namespace holding the certificate for the hostname.
                        type: string
                    type: object
                    x-kubernetes-validations:
                    - message: exactly one of secretName or issuer must be set
                      rule: has(self.secretName) != has(self.issuer)
                type: object
                x-kubernetes-validations:
                - message: 'tls requires hostname: a certificate cannot be brought
                    or issued for an auto-derived hostname'
                  rule: '!has(self.tls) || (has(self.hostname) && self.hostname !=
                    "")'
                - message: gatewayRef and ingressClassName name two different front
                    doors; set one -- gatewayRef attaches to a Gateway API Gateway,
                    ingressClassName renders an Ingress
                  rule: '!has(self.gatewayRef) || !has(self.ingressClassName) || self.ingressClassName
                    == ""'
                - message: 'with gatewayRef the Gateway''s HTTPS listener owns the
                    certificate: attach to a listener that already serves the hostname,
                    or set tls.issuer to have a certificate issued for the listener
                    to reference'
                  rule: '!has(self.gatewayRef) || !has(self.tls) || !has(self.tls.secretName)'
                - message: 'reachability: public declares an address the internet
                    reaches, but with enabled: false the platform is reached only
                    through kubectl port-forward from the machine running it; set
                    enabled: true, or leave reachability at auto'
                  rule: '!has(self.reachability) || self.reachability != "public"
                    || self.enabled'
              license:
                description: |-
                  license delivers the deployment's license key -- inline or by Secret
                  reference (at most one). Without it, Planton runs in Community mode.
                properties:
                  key:
                    description: |-
                      key is the license key pasted verbatim (the compact signed token from
                      the purchase email). Convenient for a first install; prefer
                      secretKeyRef in GitOps trees so the key never lives in the CR.
                    type: string
                  secretKeyRef:
                    description: |-
                      secretKeyRef points at an entry in an existing Secret (same
                      namespace) holding the license key. Rotating the Secret's value
                      re-delivers the key on the next control-plane restart -- a renewal is
                      a Secret edit, never a reinstall.
                    properties:
                      key:
                        description: key within the Secret.
                        minLength: 1
                        type: string
                      name:
                        description: name of the Secret.
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                type: object
                x-kubernetes-validations:
                - message: set at most one of key or secretKeyRef
                  rule: '!has(self.key) || !has(self.secretKeyRef)'
              prerequisites:
                description: |-
                  prerequisites controls how the operator deploys its own dependencies
                  (sub-operators like CloudNativePG). Defaults to auto-detection.
                properties:
                  postgresBackupPlugin:
                    default: auto
                    description: |-
                      postgresBackupPlugin controls deployment of the Barman Cloud plugin,
                      CloudNativePG's backup engine, which serves every PostgreSQL on the
                      cluster -- the platform's own database and any database deployed
                      through Planton.
                      "auto" (default): the operator installs the plugin whenever it
                      installed CloudNativePG itself and cert-manager is on the cluster (the
                      plugin needs it for the TLS between operator and plugin), or whenever
                      spec.database.postgresql.backup is declared. A cluster that cannot run
                      the plugin pays nothing for it; a plugin installed by any other means
                      is detected and respected.
                      "skip": assume already installed (or unwanted), do not deploy.
                    enum:
                    - auto
                    - skip
                    type: string
                  postgresOperator:
                    default: auto
                    description: |-
                      postgresOperator controls deployment of the CloudNativePG operator
                      (the engine behind the platform's PostgreSQL cluster).
                      "auto" (default): detect if installed, deploy if missing. A
                      CloudNativePG installed by any other means (Helm, GitOps) is detected
                      and respected automatically.
                      "skip": assume already installed, do not deploy.
                    enum:
                    - auto
                    - skip
                    type: string
                  tektonPipelines:
                    default: auto
                    description: |-
                      tektonPipelines controls deployment of Tekton Pipelines (the build
                      engine behind spec.build).
                      "auto" (default): detect if installed, deploy if missing.
                      "skip": assume already installed, do not deploy. The operator still
                      points Tekton's CloudEvents sink at the runner webhook -- sink wiring
                      is the build capability's job regardless of who installed Tekton.
                    enum:
                    - auto
                    - skip
                    type: string
                type: object
              remoteRunners:
                description: |-
                  remoteRunners lets runners OUTSIDE this cluster -- a developer's laptop
                  deploying with the cloud sign-in already on it, an appliance in another
                  network -- pull deploy work from this install. Off by default; every
                  field is optional. The in-cluster runner is unaffected either way.
                properties:
                  enabled:
                    default: false
                    description: |-
                      enabled admits runners outside this cluster and advertises the front
                      door's address to them. Default false: an install that has not chosen
                      this admits only its in-cluster runner, and a runner that asks to enroll
                      from outside is refused with the reason -- never handed an address it
                      cannot reach.
                    type: boolean
                type: object
              runner:
                description: |-
                  runner configures the in-cluster worker that executes IaC deployments.
                  Deployed by default so a fresh install can deploy real infrastructure
                  out of the box; every field is optional.
                properties:
                  cloudCredentialsSecretName:
                    description: |-
                      cloudCredentialsSecretName references a Secret in the same namespace
                      whose keys are injected into the runner pod as environment variables
                      (e.g. AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY). The path for clusters
                      without workload identity: the Secret stays yours, in your cluster --
                      the platform never stores cloud credentials. Prefer
                      serviceAccountAnnotations where the cluster supports it.
                    type: string
                  enabled:
                    default: true
                    description: |-
                      enabled controls whether the in-cluster runner is deployed. Default
                      true: a Planton that cannot deploy infrastructure is a browsing UI, so
                      opting OUT is the deliberate act (e.g. an install that only uses
                      externally registered runners).
                    type: boolean
                  image:
                    description: image overrides the default runner container image.
                    properties:
                      repository:
                        description: |-
                          repository is the full container image repository
                          (e.g., "ghcr.io/plantonhq/planton/control-plane").
                        type: string
                      tag:
                        description: tag overrides the image tag. Defaults to spec.version
                          when empty.
                        type: string
                    type: object
                  serviceAccountAnnotations:
                    additionalProperties:
                      type: string
                    description: |-
                      serviceAccountAnnotations are applied to the runner pod's dedicated
                      Kubernetes ServiceAccount. This is the workload-identity seam: annotate
                      with your cloud's binding (EKS "eks.amazonaws.com/role-arn", GKE
                      "iam.gke.io/gcp-service-account", AKS "azure.workload.identity/client-id")
                      and the runner's deploys use that ambient identity -- no cloud keys
                      stored anywhere, in the platform or the cluster.
                    type: object
                  storageClassName:
                    description: |-
                      storageClassName pins the IaC-state volume to a StorageClass. Defaults
                      to spec.storage.storageClassName, then the cluster default. Applied
                      when the volume is first created; an existing volume keeps its class.
                    type: string
                  storageSize:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      storageSize is the persistent volume holding IaC state: the default
                      state backend keeps each resource's OpenTofu state file here. Defaults
                      to spec.storage.size, then 2Gi. Honest limits: state durability equals
                      this volume's durability -- back it up like you would any state file,
                      or graduate to an S3/GCS/Azure/R2 bucket StateBackend (or a Terraform
                      Cloud remote backend) through the console for managed-grade durability.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                type: object
              storage:
                description: |-
                  storage sets platform-wide storage defaults for every persistent
                  volume the operator creates: the StorageClass volumes are provisioned
                  from and one size applied across all of them. Component settings
                  (e.g. spec.database.postgresql.storageSize) override these per volume.
                properties:
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      size is applied to every persistent volume unless a component sets
                      its own storageSize.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: |-
                      storageClassName is the StorageClass every persistent volume is
                      provisioned from unless a component names its own. When empty, volumes
                      use the cluster's default StorageClass.
                    type: string
                type: object
              vault:
                description: |-
                  vault configures the bundled secrets manager (OpenBAO, the open-source
                  Vault fork). Deployed by default: it is integral the way the database
                  is -- it backs the credential store for pasted connection secrets, the
                  default envelope-encryption key, and the OIDC issuer's signing key
                  (keyless connections). Every field is optional; a zero-config install
                  gets an initialized, unsealed vault with its engines mounted and the
                  platform secret backend seeded as the org default. Choosing a cloud
                  secret backend instead is a layered choice, not a reason to opt out.
                  The vault stores its data in the platform's own PostgreSQL, so
                  spec.database.postgresql.backup archives it with the records; what
                  opens the restored vault is declared here (autoUnseal, initSecretName).
                properties:
                  autoUnseal:
                    description: |-
                      autoUnseal delegates the vault's master-key protection to a key in
                      the adopter's cloud (or a central OpenBao/Vault's transit engine), so
                      the vault unseals itself on every start -- including the start after a
                      restore, on a cluster that has never seen it. Unset, the vault uses the
                      built-in key shares and the operator unseals it with the shares it
                      wrote to the init Secret; a restore then needs that Secret present.
                      Exactly one seal arm is set, and its key and grants must exist BEFORE
                      the platform: the seal is checked at server start, not at init, so a
                      missing key or role crash-loops the vault before anything else can
                      happen.
                    properties:
                      awsKms:
                        description: |-
                          OpenBAOAwsKmsSealSpec: an AWS KMS key. Natural on EKS with IRSA on the
                          vault's ServiceAccount; static keys are the fallback.
                        properties:
                          accessKeyId:
                            description: |-
                              accessKeyId is the public half of a static key pair -- an identifier,
                              not a credential. Set only with credentialsSecretName; empty means the
                              vault's pods inherit an IAM role (IRSA, EKS Pod Identity, an instance
                              profile), the preferred posture.
                            type: string
                          credentialsSecretName:
                            description: |-
                              credentialsSecretName names a Secret in the platform's namespace with
                              key AWS_SECRET_ACCESS_KEY, the secret half of the static key pair.
                              Empty means keyless.
                            type: string
                          kmsKeyId:
                            description: |-
                              kmsKeyId is the key id, alias, or full ARN of a SYMMETRIC
                              encrypt/decrypt key.
                            minLength: 1
                            type: string
                          region:
                            description: region of the KMS key.
                            minLength: 1
                            type: string
                        required:
                        - kmsKeyId
                        - region
                        type: object
                      azureKeyVault:
                        description: |-
                          OpenBAOAzureKeyVaultSealSpec: a key in an Azure Key Vault. Natural on AKS
                          with Workload Identity or a managed identity on the vault's
                          ServiceAccount; a service principal is the fallback.
                        properties:
                          clientId:
                            description: |-
                              clientId is a service principal's client id -- an identifier, not a
                              credential. Set only with credentialsSecretName; empty means the
                              vault's pods carry an Azure identity, the preferred posture.
                            type: string
                          credentialsSecretName:
                            description: |-
                              credentialsSecretName names a Secret in the platform's namespace with
                              key AZURE_CLIENT_SECRET, the service principal's secret. Empty means
                              keyless.
                            type: string
                          keyName:
                            description: keyName is the key inside the vault.
                            minLength: 1
                            type: string
                          tenantId:
                            description: tenantId is the Entra (Azure AD) tenant.
                            minLength: 1
                            type: string
                          vaultName:
                            description: vaultName is the Key Vault (the vault, not
                              the key).
                            minLength: 1
                            type: string
                        required:
                        - keyName
                        - tenantId
                        - vaultName
                        type: object
                      gcpKms:
                        description: |-
                          OpenBAOGcpKmsSealSpec: a Google Cloud KMS key. Keyless by construction --
                          the vault's ServiceAccount carries the identity through GKE Workload
                          Identity (serviceAccountAnnotations), or the node's ambient credentials
                          apply. The identity needs TWO roles on the key: cryptoKeyEncrypterDecrypter
                          to wrap on init and unwrap on every unseal, AND cloudkms.viewer for the
                          key-existence check the server makes when it configures the seal at start;
                          with only the first, the pod crash-loops on "Permission
                          'cloudkms.cryptoKeys.get' denied" and init never opens.
                        properties:
                          cryptoKey:
                            description: |-
                              cryptoKey is the symmetric encrypt/decrypt key that wraps the master
                              key.
                            minLength: 1
                            type: string
                          keyRing:
                            description: keyRing is the key ring's name.
                            minLength: 1
                            type: string
                          project:
                            description: project that holds the key ring.
                            minLength: 1
                            type: string
                          region:
                            description: region of the key ring ("global", "us-central1",
                              ...).
                            minLength: 1
                            type: string
                        required:
                        - cryptoKey
                        - keyRing
                        - project
                        - region
                        type: object
                      transit:
                        description: |-
                          OpenBAOTransitSealSpec: the transit engine of a central OpenBao or Vault.
                          The vault depends on the central instance being reachable and unsealed at
                          every start; the engine must exist before this platform (the key may be
                          born on the first encrypt), or the pod crash-loops on "Error configuring
                          seal".
                        properties:
                          address:
                            description: address of the central instance ("https://bao.example.com:8200").
                            minLength: 1
                            type: string
                          credentialsSecretName:
                            description: |-
                              credentialsSecretName names a Secret in the platform's namespace with
                              key VAULT_TOKEN, a token authorized to encrypt and decrypt on the
                              transit key. Empty means the central instance is reached without one.
                            type: string
                          keyName:
                            description: keyName is the transit key that wraps the
                              master key.
                            minLength: 1
                            type: string
                          mountPath:
                            default: transit/
                            description: mountPath is the transit engine's mount on
                              the central instance.
                            type: string
                        required:
                        - address
                        - keyName
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: 'autoUnseal names exactly one seal: awsKms, gcpKms,
                        azureKeyVault, or transit'
                      rule: '[has(self.awsKms), has(self.gcpKms), has(self.azureKeyVault),
                        has(self.transit)].filter(x, x).size() == 1'
                  enabled:
                    default: true
                    description: |-
                      enabled controls whether the bundled secrets manager is deployed.
                      Default true: a Planton without a secrets store cannot hold pasted
                      connection credentials or serve keyless connections, so opting OUT is
                      the deliberate act (e.g. an install that runs exclusively on an
                      ambient-authenticated cloud secret backend and accepts losing those
                      capabilities).
                    type: boolean
                  initSecretName:
                    description: |-
                      initSecretName names a Secret the ADOPTER owns, in the platform's
                      namespace, where the operator writes the vault's keys at
                      initialization: the unseal keys (recovery keys under autoUnseal) and
                      the root token. The operator creates it WITHOUT an owner reference and
                      never deletes it, so deleting the PlantonPlatform leaves it standing --
                      deleting the namespace does not, and a namespace the declaring resource
                      owns goes with that resource. On a restore the operator reads the
                      shares from it to unseal a built-in-seal vault, and under any seal it
                      is the vault's break-glass. It is the one object a lost cluster takes
                      with it that no archive brings back -- keep a copy outside the
                      cluster. Empty means the operator keeps the keys in a Secret it owns
                      ({platform}-openbao-init), deleted with the platform.
                    type: string
                  serviceAccountAnnotations:
                    additionalProperties:
                      type: string
                    description: |-
                      serviceAccountAnnotations go on the ServiceAccount the vault's pods run
                      as (the operator names it "{platform}-openbao"). This is where a
                      keyless seal identity binds: EKS "eks.amazonaws.com/role-arn" for an
                      AWS KMS seal, AKS "azure.workload.identity/client-id" for a Key Vault
                      seal, GKE "iam.gke.io/gcp-service-account" for a Cloud KMS seal. The
                      identity only reaches the seal key; the runner's, the control plane's,
                      and the database backup's identities are their own.
                    type: object
                type: object
                x-kubernetes-validations:
                - message: 'vault.enabled: false opts out of the bundled secrets manager;
                    remove autoUnseal, initSecretName, and serviceAccountAnnotations,
                    or re-enable the vault'
                  rule: '!has(self.enabled) || self.enabled || (!has(self.autoUnseal)
                    && (!has(self.initSecretName) || size(self.initSecretName) ==
                    0) && (!has(self.serviceAccountAnnotations) || size(self.serviceAccountAnnotations)
                    == 0))'
              version:
                description: |-
                  version is the Planton platform release to deploy, as vMAJOR.MINOR.PATCH
                  (a pre-release suffix is allowed). The control plane, console, and runner
                  run this version as one coherent line; changing it is how the platform
                  upgrades. The operator refuses a release older than the oldest it runs
                  and says so in status (VersionSupported). To run a custom build, keep
                  version at a release and set image.tag on the component: the version
                  names the contract, the tag names the bytes.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: spec.version must name a Planton release as vMAJOR.MINOR.PATCH
                    (a pre-release suffix is allowed); to run a custom build, keep
                    version at a release and set image.tag on the component
                  rule: self.matches('^v[0-9]+\\.[0-9]+\\.[0-9]+(-[0-9A-Za-z.-]+)?(\\+[0-9A-Za-z.-]+)?$')
            required:
            - version
            type: object
            x-kubernetes-validations:
            - message: 'bootstrap.secretBackend type ''platform'' stores secrets in
                the bundled vault, which spec.vault.enabled: false has opted out of;
                re-enable the vault or use type awsSecretsManager'
              rule: '!has(self.bootstrap) || !has(self.bootstrap.secretBackend) ||
                self.bootstrap.secretBackend.type != ''platform'' || !has(self.vault)
                || !has(self.vault.enabled) || self.vault.enabled'
            - message: 'remoteRunners.enabled admits runners outside the cluster through
                the front door, but with ingress disabled there is no front door a
                laptop could reach; set ingress.enabled: true with a gatewayRef, or
                leave remoteRunners off'
              rule: '!has(self.remoteRunners) || !has(self.remoteRunners.enabled)
                || !self.remoteRunners.enabled || (has(self.ingress) && self.ingress.enabled)'
            - message: a backup carries the vault's data, but under the built-in seal
                the vault's keys live in a Secret that is deleted with the platform;
                set vault.initSecretName to a Secret you own (and keep a copy outside
                the cluster), or declare vault.autoUnseal so a restored vault opens
                from your cloud key
              rule: '!has(self.database) || !has(self.database.postgresql) || !has(self.database.postgresql.backup)
                || (has(self.vault) && has(self.vault.enabled) && !self.vault.enabled)
                || (has(self.vault) && (has(self.vault.autoUnseal) || (has(self.vault.initSecretName)
                && size(self.vault.initSecretName) > 0)))'
          status:
            description: PlantonPlatformStatus defines the observed state of a PlantonPlatform
              deployment.
            properties:
              backup:
                description: |-
                  backup is what the operator knows about the platform database's
                  backup (see BackupStatus): the one-word state the Backup column prints,
                  the server name a recovery copies, the recoverability point, and the
                  last base backup. Read from the database operator's own conditions.
                properties:
                  firstRecoverabilityPoint:
                    description: |-
                      firstRecoverabilityPoint is the earliest point in time the archive can
                      restore to (the start of the oldest base backup still retained).
                    format: date-time
                    type: string
                  lastFailedBackup:
                    description: lastFailedBackup is when a base backup last failed,
                      if one has.
                    format: date-time
                    type: string
                  lastSuccessfulBackup:
                    description: lastSuccessfulBackup is when the most recent base
                      backup completed.
                    format: date-time
                    type: string
                  message:
                    description: |-
                      message says why the state is what it is, in plain language -- the
                      plugin's own sentence when it is the one that knows.
                    type: string
                  restoredFrom:
                    description: |-
                      restoredFrom is the source archive's server name when this platform's
                      database was bootstrapped from an archive (spec.database.postgresql.recoverFrom
                      honored at creation), read from the live database for as long as it
                      lives. Empty for a database created empty. The identity component
                      keys on it: a restored realm carries its source's admin credential,
                      which the operator re-establishes exactly once.
                    type: string
                  serverName:
                    description: |-
                      serverName is the name this platform's archive is filed under in the
                      store -- what a recovery declaration copies. Set once backups are
                      declared.
                    type: string
                  state:
                    description: state is the one-word answer; see BackupState.
                    type: string
                  vault:
                    description: |-
                      vault is whether this archive carries the bundled secrets manager, and
                      what opens the restored vault. Present whenever the operator can say;
                      absent while it cannot yet.
                    properties:
                      covered:
                        description: |-
                          covered is whether the archive carries the vault's data: true when the
                          vault runs and a backup is declared; false when the vault is opted out
                          (nothing to archive) or no backup is declared.
                        type: boolean
                      initSecretName:
                        description: |-
                          initSecretName is the Secret holding the vault's keys and root token
                          -- the one object to keep a copy of outside the cluster. Under
                          "shamir" a restore needs it present; under a cloud seal it is the
                          break-glass. The adopter's own when spec.vault.initSecretName is set,
                          otherwise the operator's, deleted with the platform.
                        type: string
                      message:
                        description: |-
                          message says, in plain language, what a restore brings back and what
                          it needs from the person -- which Secret to keep, which key must
                          exist.
                        type: string
                      seal:
                        description: |-
                          seal is what opens the vault: "shamir" (the built-in key shares, held
                          in the init Secret), or the cloud seal declared on spec.vault.autoUnseal
                          -- "awsKms", "gcpKms", "azureKeyVault", "transit". Deliberately a word,
                          not an enum, for the reason BackupState is not one.
                        type: string
                    required:
                    - covered
                    type: object
                required:
                - state
                type: object
              components:
                description: components reports the status of each individual component.
                properties:
                  console:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  controlPlane:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  gateway:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  identity:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  ingress:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  neo4j:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  openBao:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  openFGA:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  postgresql:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  redis:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  runner:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  tekton:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                  temporal:
                    description: |-
                      ComponentStatus describes the observed state of an individual component:
                      its phase, the one-word reason behind it, the object the reason is about,
                      and the sentence a person acts on. A component that is stuck never says
                      only "waiting" -- the reason and message name what is wrong and what to do.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is when the phase, reason, or object last changed.
                          It does not move while the same condition persists.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message provides human-readable detail about the component's current
                          state: what was observed, what it most likely means, and the exact
                          next step.
                        type: string
                      object:
                        description: |-
                          object names the Kubernetes object the reason is about -- the Pod whose
                          image cannot be pulled, the PersistentVolumeClaim that will not
                          provision -- so `kubectl describe` on it is the next step. Absent when
                          the reason is about the component as a whole.
                        properties:
                          kind:
                            description: kind is the object's kind (Pod, PersistentVolumeClaim,
                              Deployment, Job).
                            type: string
                          name:
                            description: name is the object's name.
                            type: string
                          namespace:
                            description: |-
                              namespace is set only when the object lives outside the platform's own
                              namespace (a sub-operator's controller, for example).
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      phase:
                        description: phase is the lifecycle phase of this component.
                        enum:
                        - Pending
                        - Deploying
                        - Ready
                        - Error
                        type: string
                      reason:
                        description: |-
                          reason is the one-word, machine-readable cause behind the phase (for
                          example ImagePullFailed, CrashLooping, VolumeUnprovisionable, Healthy).
                          Each reason has a row in the troubleshooting reference.
                        type: string
                    required:
                    - phase
                    type: object
                type: object
              conditions:
                description: |-
                  conditions represent the latest available observations of the deployment's state.
                  Condition types: Ready (all enabled components healthy) and VersionSupported
                  (spec.version names a release this operator runs).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consoleUrl:
                description: |-
                  consoleUrl is the URL the web console is reachable at once ingress is
                  configured and admitted. The platform tells you your URL -- with an
                  auto-derived hostname this is where the derived address is published.
                type: string
              email:
                description: |-
                  email echoes which provider arm spec.email declares (NotConfigured,
                  SMTP, or Resend) -- configuration echo like license, feeding the
                  kubectl column. Whether the relay ACCEPTS mail is the control plane's
                  own answer, checked on demand from its settings page, never guessed
                  here: the operator has no channel to the relay and must not probe it.
                type: string
              github:
                description: |-
                  github echoes the declared GitHub hosts and which carry an install App,
                  e.g. "github.example.com (App), github.com" -- configuration echo like
                  email, feeding the kubectl column. NotConfigured when spec.github is
                  absent. Whether GitHub accepts the App's credentials is the control
                  plane's answer at connection time, never guessed here.
                type: string
              license:
                description: |-
                  license echoes how the license key is delivered to the control plane
                  (Community, InlineKey, or SecretRef) -- configuration echo like
                  version, feeding the kubectl column. Whether the key VERIFIES and
                  what it grants is the control plane's own answer, served by its
                  entitlements advertisement, never guessed here.
                type: string
              phase:
                description: phase is the high-level lifecycle phase of the entire
                  deployment.
                enum:
                - Pending
                - Deploying
                - Ready
                - Error
                - Upgrading
                type: string
              reachability:
                description: |-
                  reachability is what the operator concluded about whether the public
                  internet reaches the front door: "public" or "private", never "auto".
                  Published beside consoleUrl by the component that owns the door, so a
                  person who left spec.ingress.reachability at its default reads the
                  answer next to the address it is an answer about. Every internet-facing
                  posture the control plane advertises (the keyless identity issuer,
                  GitHub webhook delivery) derives from this one word.
                enum:
                - auto
                - public
                - private
                type: string
              requiredOperatorVersion:
                description: |-
                  requiredOperatorVersion is the oldest operator release the declared
                  platform release says it needs, as its published image declares it;
                  empty when the release declares nothing or the registry could not be
                  read. When it names a release newer than the running operator, the
                  VersionSupported condition says so and nothing is rendered.
                type: string
              version:
                description: version is the currently deployed Planton platform version.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
