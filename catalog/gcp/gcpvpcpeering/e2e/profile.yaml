apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpvpcpeering
spec:
  # Awaiting the live proof lane. Offline bar met: both modules build and
  # validate, offline plans for the reference manifest, both scenarios, and the
  # two fixture manifests are clean on the pinned provider, and the accounting
  # is total. Live facts the proof lane confirms: (1) side A (under test) and
  # the side-B fixture on the second network both reach ACTIVE -- the verifier
  # polls the network's peering entry for the state; (2) the four route flags
  # round-trip and RE-PLAN CLEAN under the apply-idempotency gate, including
  # CONSENSUS; (3) the harness destroys side A before the fixture networks.
  # Project-scoped; needs only roles/compute.networkAdmin on the harness
  # project.
  #
  # Offline-proven-only arm (skips honestly through its required-env token):
  #   - routes-config-existing-peering: the ROUTES-CONFIG form targets the
  #     Google-managed servicenetworking-googleapis-com peering, which exists
  #     only behind a private-services-access connection plus a Cloud SQL or
  #     Memorystore instance -- a fixture the harness does not carry. Unblock:
  #     a private-IP fixture exported as PLANTON_E2E_GCP_SERVICE_NETWORKING_VPC.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 20
