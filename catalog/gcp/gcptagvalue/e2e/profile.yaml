apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcptagvalue
spec:
  # Awaiting the live proof lane. Offline bar met: both modules build and
  # validate, offline plans for the reference manifest and the scenario are
  # clean on the pinned provider, and the accounting is total. Live facts
  # the proof lane confirms: (1) the reference to the prerequisite GcpTagKey
  # resolves to tagKeys/{id} on both engines; (2) the derived tag_value_id
  # matches the name; (3) the harness destroys the value before the key
  # (Google refuses the other order). Project-owned chain; needs only
  # roles/resourcemanager.tagAdmin on the harness project.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
