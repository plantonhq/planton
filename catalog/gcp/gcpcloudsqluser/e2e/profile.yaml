apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpcloudsqluser
spec:
  # Composed scenario: deploys the GcpCloudSql prerequisite then creates a
  # BUILT_IN user on it.
  tier: 1
  # Live-proven 2026-08-13 on both engines at the total-accounting spec:
  # minimal scenario (BUILT_IN user + database_roles [cloudsqlsuperuser]
  # accepted by the live API + explicit deletionPolicy DELETE) x 2
  # engines, all phases green under the apply-idempotency gate.
  #
  # Awaiting re-proof: the user gained service_account: an IAM service-account
  # user wired by reference to a GcpServiceAccount, with both engines deriving
  # the database username Cloud SQL expects. The new iam-service-account
  # scenario (a new GcpServiceAccount prerequisite,
  # cloudsql.iam_authentication=on on the prerequisite instance) proves the
  # derived name exists under the account's identity and re-plans clean,
  # beside the BUILT_IN smoke.
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
