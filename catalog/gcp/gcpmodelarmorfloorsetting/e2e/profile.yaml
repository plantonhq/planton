apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpmodelarmorfloorsetting
spec:
  # Awaiting the live proof lane. The scenario proves on the harness
  # project's own floor with roles/modelarmor.floorSettingsAdmin (folder
  # and organization floors are proven offline: they need organization
  # roles the harness does not hold). Offline bar met: both modules build
  # and validate, offline plans for the reference manifest, the scenario,
  # and throwaway folder and organization arms are clean on the pinned
  # provider (the empty scope reads google_client_config, credential-free),
  # and the accounting is total. Live facts the proof lane confirms: (1)
  # the PATCH applies over whatever floor the project already had and
  # RE-PLANS CLEAN on both engines -- only the chosen inspect flag is sent;
  # (2) the name output is {parent}/locations/global/floorSetting; (3)
  # destroy leaves the floor in Google and the verifier reads it after
  # destroy; (4) the floor left behind (inspect-only, injection detection
  # at HIGH) blocks no other lane and no template scenario.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
