# Least-privilege permissions the IaC runner's GCP principal needs to
# provision, update, and destroy this component with the official modules.
# Every entry is `derived` (static analysis of the module sources, cited in
# notes) until the live capture lane upgrades it to `proven`. Schema:
# iac/componentpermissions/v1.
apiVersion: iac.planton.dev/v1
kind: ComponentPermissions
metadata:
  name: gcpbigquerycapacitycommitment
spec:
  gcp:
    groups:
      - purpose: EnableApi
        permissions:
          - serviceusage.services.enable
          - serviceusage.services.get
        provenance: derived
        notes: Covers google_project_service enabling bigqueryreservation.googleapis.com on the target project. disable_on_destroy is false in the module, so the disable permission is never needed.
      - purpose: ManageCommitment
        permissions:
          - bigquery.capacityCommitments.create
          - bigquery.capacityCommitments.get
          - bigquery.capacityCommitments.update
          - bigquery.capacityCommitments.delete
        provenance: derived
        notes: Covers google_bigquery_capacity_commitment (Pulumi bigquery.CapacityCommitment) -- purchased, its plans patched, read on every refresh, deleted (refused by Google before the term ends). Names grounded in Google's BigQuery roles page.
