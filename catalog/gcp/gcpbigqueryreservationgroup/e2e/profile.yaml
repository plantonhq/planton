apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpbigqueryreservationgroup
spec:
  # Awaiting the live proof lane. The scenario proves in the harness project
  # with roles/bigquery.resourceAdmin: one empty group in US, free. Offline
  # bar met: both modules build and validate, offline plans for the
  # reference manifest, every scenario, and every fixture are clean on the
  # pinned provider, and the accounting is total. Live facts the proof lane
  # confirms: (1) the group creates and its full name reads back (the id is
  # the full path, the provider's name the short one); (2) reservation
  # groups may be allowlisted -- a 403 is recorded, not patched; (3) destroy
  # deletes the group and the verifier reads 404.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
