# Provider-parity manifest: this kind's recorded judgment against the pinned
# google provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  google_vertex_ai_rag_engine_config:
    mappings:
      - spec: spec.project_id
        arg: project
      # The provider names the axis region; every Vertex kind in the
      # catalog calls it location, and the modules pin one to the other.
      - spec: spec.location
        arg: region
  google_project_service:
    internal: >-
      API enablement plumbing -- the module enables the owning API so a
      fresh project works on the first deploy; not a knob of the kind.

specExclusions:
  - field: spec.tier
    reason: >-
      the provider expresses the tier as exactly one of three EMPTY marker
      blocks under rag_managed_db_config (basic / scaled / unprovisioned)
      with no settings of their own; the spec carries the choice as an enum
      and both modules emit the matching block (the established
      marker-block judgment, here as a selector over three markers)
