apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpbigqueryreservation
spec:
  # Awaiting the live proof lane. Both scenarios prove in the harness
  # project with roles/bigquery.resourceAdmin; the module enables
  # bigqueryreservation.googleapis.com itself: a zero-baseline Standard
  # reservation autoscaling to 50 slots with the harness project assigned
  # for QUERY, and a zero-baseline Enterprise reservation joining a
  # reservation group fixture. Offline bar met: both modules build and
  # validate, offline plans for the reference manifest, every scenario, and
  # every fixture are clean on the pinned provider, and the accounting is
  # total. Live facts the proof lane confirms: (1) slot_capacity 0 with
  # autoscaling is accepted and bills nothing idle; (2) the assignment
  # reaches ACTIVE (or stays PENDING) with autoscale-only capacity --
  # recorded either way; (3) the assignment's full name reads back and the
  # verifier finds it in the reservation's assignment list; (4) reservation
  # groups may be allowlisted -- a 403 there is recorded, not patched; (5)
  # destroy deletes the assignments before the reservation.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 30
