# Provider-parity manifest: this kind's recorded judgment against the pinned
# google provider (checked by `planton provider-parity --check` and the
# lint.provider-parity CI lane; format documented in pkg/providerparity).
#
# Only divergences are recorded -- an argument absent from this file must
# match its spec field by exact name. Every exclusion carries its reason:
# omission is a decision, never an accident.

resources:
  google_colab_runtime_template:
    mappings:
      - spec: spec.project_id
        arg: project
      # The provider's `name` is the template's id.
      - spec: spec.runtime_template_id
        arg: name
      # One-leaf wrappers lifted where the leaf name stands alone; each
      # block is sent only when its field is set.
      - spec: spec.idle_timeout
        arg: idle_shutdown_config.idle_timeout
      - spec: spec.euc_disabled
        arg: euc_config.euc_disabled
      - spec: spec.enable_secure_boot
        arg: shielded_vm_config.enable_secure_boot
      - spec: spec.kms_key_name
        arg: encryption_spec.kms_key_name
  google_project_service:
    internal: >-
      API enablement plumbing -- the module enables the owning API so a
      fresh project works on the first deploy; not a knob of the kind.
