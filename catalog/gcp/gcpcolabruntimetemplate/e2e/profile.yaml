apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: gcpcolabruntimetemplate
spec:
  # Awaiting the live proof lane. The scenario proves in the harness
  # project with roles/aiplatform.admin (Colab Enterprise runs on the
  # Vertex AI API); the module enables aiplatform.googleapis.com itself.
  # Offline bar met: both modules build and validate, offline plans for
  # the reference manifest, the scenario, and the private-network, GPU,
  # and CMEK arms are clean on the pinned provider, and the accounting is
  # total. Live facts the proof lane confirms: (1) the template RE-PLANS
  # CLEAN on both engines -- the attribution labels (fixed at creation,
  # effective_labels ForceNew) show no diff and force no replacement, and
  # the Optional+Computed machine, disk, idle, and image settings left
  # unset show none; (2) the disk size string round-trips; (3) destroy
  # deletes the template and the verifier reads 404.
  tier: 1
  status: pending_proof
  validated_provisioners: []
  timeout_minutes: 15
