# The same document at v1alpha2, canonical: renamed, reshaped, defaulted --
# and minus the declared loss. The corpus laws compare in the canonical
# domain, so the 64-bit values may be authored naturally here (canonical form
# carries them as strings; the comparison absorbs the representation).
apiVersion: _test.planton.dev/v1alpha2
kind: TestCloudResourceGeneric
metadata:
  name: torture-storage-spelled
spec:
  displayName: renamed-on-upgrade
  int32Field: 7
  int64Field: 9999
  uint64Field: 50000
  boolField: true
  nested:
    nestedString: passes-through-untouched
  requiredRef:
    value: literal-required-value
  labels:
    purpose: corpus
  steps:
    - command: first
    - command: second
  replicas: 1
  sensitiveString: $secret/not-a-real-secret
