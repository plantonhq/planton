name: kubernetes-open-bao
runtime: go
description: OpenBao secrets manager from the official chart — dev, or a storage engine (integrated Raft, or PostgreSQL by reference to a KubernetesPostgres) and a replica count, with module-synthesized HCL config (the engine's storage stanza, retry_join peers, TLS listener, auto-unseal seal stanzas, telemetry) driving the chart's own mode, the opt-in agent injector, and module-owned scheduled Raft snapshots to S3, GCS, Azure Blob, or Cloudflare R2 with a declared restore
