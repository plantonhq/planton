name: kubernetes-open-bao
runtime: go
description: OpenBao secrets manager from the official chart — dev/standalone/HA-Raft server modes with module-synthesized HCL config (retry_join peers, TLS listener, auto-unseal seal stanzas, telemetry), and the opt-in agent injector
