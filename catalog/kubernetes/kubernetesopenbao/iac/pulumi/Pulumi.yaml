name: kubernetes-open-bao
runtime: go
description: OpenBao secrets manager from the official chart — dev/standalone/HA-Raft server modes with module-synthesized HCL config (retry_join peers, TLS listener, auto-unseal seal stanzas, telemetry), the opt-in agent injector, and module-owned scheduled Raft snapshots to S3, GCS, Azure Blob, or Cloudflare R2 with a declared restore
