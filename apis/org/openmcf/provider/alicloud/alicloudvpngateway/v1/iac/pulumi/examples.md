# AliCloudVpnGateway Pulumi Examples

See [../../examples.md](../../examples.md) for YAML-based usage examples.

The Pulumi module is invoked via the standard OpenMCF stack-input mechanism. The YAML manifests are loaded by the entrypoint and passed to the module's `Resources()` function.
