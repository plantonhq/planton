apiVersion: qa.openmcf.org/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetescertificate
spec:
  tier: 4
  status: skip
  deferred_reason: "requires a configured Issuer or ClusterIssuer on the target cluster"
  validated_provisioners:
    - pulumi
  timeout_minutes: 10
