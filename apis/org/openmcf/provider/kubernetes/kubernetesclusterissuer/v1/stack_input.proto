syntax = "proto3";

package org.openmcf.provider.kubernetes.kubernetesclusterissuer.v1;

import "org/openmcf/provider/kubernetes/kubernetesclusterissuer/v1/api.proto";
import "org/openmcf/provider/kubernetes/provider.proto";

// Input for the KubernetesClusterIssuer IaC stack.
message KubernetesClusterIssuerStackInput {
  // Target cloud-resource.
  KubernetesClusterIssuer target = 1;
  // Kubernetes provider configuration (cluster credentials).
  org.openmcf.provider.kubernetes.KubernetesProviderConfig provider_config = 2;
}
