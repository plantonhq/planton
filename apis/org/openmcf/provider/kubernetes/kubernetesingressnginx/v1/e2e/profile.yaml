apiVersion: qa.openmcf.org/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesingressnginx
spec:
  tier: 4
  status: deferred
  deferred_reason: "admission webhook + LoadBalancer timeout on kind"
  validated_provisioners:
    - pulumi
  limitations:
    - "LoadBalancer stays Pending on kind"
  timeout_minutes: 15
