apiVersion: kubernetes.openmcf.org/v1
kind: KubernetesTekton
metadata:
  name: my-tekton
spec:
  namespace:
    value: tekton-pipelines
  createNamespace: true
  container:
    resources:
      requests:
        cpu: 50m
        memory: 100Mi
      limits:
        cpu: 1000m
        memory: 1Gi
