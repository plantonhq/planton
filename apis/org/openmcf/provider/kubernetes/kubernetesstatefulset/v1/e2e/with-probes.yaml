apiVersion: kubernetes.openmcf.org/v1
kind: KubernetesStatefulSet
metadata:
  name: e2e-sts-probes
spec:
  namespace:
    value: e2e-sts-prb-ns
  create_namespace: true
  container:
    app:
      image:
        repo: nginx
        tag: "1.27-alpine"
      resources:
        requests:
          cpu: "10m"
          memory: "32Mi"
        limits:
          cpu: "100m"
          memory: "64Mi"
      ports:
        - name: http
          container_port: 80
          network_protocol: TCP
          app_protocol: http
          service_port: 80
      liveness_probe:
        http_get:
          path: /
          port_number: 80
        initial_delay_seconds: 5
        period_seconds: 10
      readiness_probe:
        http_get:
          path: /
          port_number: 80
        initial_delay_seconds: 3
        period_seconds: 5
