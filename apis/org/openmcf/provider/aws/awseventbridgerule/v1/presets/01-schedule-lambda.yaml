apiVersion: aws.openmcf.org/v1
kind: AwsEventBridgeRule
metadata:
  name: my-schedule
spec:
  region: <aws-region>
  description: Trigger Lambda function on a recurring schedule
  scheduleExpression: "rate(1 hour)"
  targets:
    - name: my-function
      arn:
        value: <lambda-function-arn>
