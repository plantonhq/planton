apiVersion: qa.openmcf.org/v1
kind: ComponentE2EProfile
metadata:
  name: awss3bucket
spec:
  # Walking skeleton for the AWS harness. Flips to `green` after the first live
  # run proves deploy -> verify -> destroy on both provisioners.
  tier: 1
  status: stub
  validated_provisioners:
    - pulumi
    - terraform
  timeout_minutes: 5
