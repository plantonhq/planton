apiVersion: aws.openmcf.org/v1
kind: AwsServerlessElasticache
metadata:
  name: ${name}
  org: ${org}
  env: ${env}
  id: ${name}-${env}
spec:
  region: us-west-2
  engine: redis
  majorEngineVersion: "7"
