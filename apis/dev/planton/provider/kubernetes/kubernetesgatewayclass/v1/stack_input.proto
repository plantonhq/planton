syntax = "proto3";

package dev.planton.provider.kubernetes.kubernetesgatewayclass.v1;

import "dev/planton/provider/kubernetes/kubernetesgatewayclass/v1/api.proto";
import "dev/planton/provider/kubernetes/provider.proto";

// Input for the KubernetesGatewayClass IaC stack.
message KubernetesGatewayClassStackInput {
  // Target cloud-resource.
  KubernetesGatewayClass target = 1;
  // Kubernetes provider configuration (cluster credentials).
  dev.planton.provider.kubernetes.KubernetesProviderConfig provider_config = 2;
}
