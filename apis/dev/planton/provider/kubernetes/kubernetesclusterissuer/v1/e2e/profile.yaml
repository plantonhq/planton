apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesclusterissuer
spec:
  tier: 4
  status: skip
  deferred_reason: "requires cloud DNS credentials (ACME + DNS provider)"
  validated_provisioners:
    - pulumi
  timeout_minutes: 15
