apiVersion: kubernetes.planton.dev/v1
kind: KubernetesPerconaMongoOperator
metadata:
  name: e2e-percona-mongo-op-minimal
spec:
  namespace:
    value: e2e-percona-mongo-op-minimal
  create_namespace: true
  container:
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 64Mi
