apiVersion: kubernetes.planton.dev/v1
kind: KubernetesTelemetry
metadata:
  name: mesh-default-tracing
spec:
  namespace:
    value: <root-namespace>
  tracing:
    - random_sampling_percentage: <sample-percentage>
      custom_tags:
        cluster:
          literal:
            value: <tag-value>
