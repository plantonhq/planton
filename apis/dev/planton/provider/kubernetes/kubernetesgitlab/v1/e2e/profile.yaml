apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesgitlab
spec:
  tier: 2
  status: deferred
  deferred_reason: "too heavy for default kind cluster"
  validated_provisioners:
    - pulumi
  timeout_minutes: 15
  limitations:
    - "requires 4-8 GB+ memory"
