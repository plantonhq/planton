apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesjenkins
spec:
  tier: 2
  status: deferred
  deferred_reason: "init timeout on kind -- too heavy for default kind"
  validated_provisioners:
    - pulumi
  timeout_minutes: 15
  limitations:
    - "requires 4+ GB RAM for Jenkins startup"
