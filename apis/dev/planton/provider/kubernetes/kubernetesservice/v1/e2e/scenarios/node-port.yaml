apiVersion: kubernetes.planton.dev/v1
kind: KubernetesService
metadata:
  name: e2e-svc-nodeport
spec:
  namespace: default
  name: e2e-svc-nodeport
  type: node_port
  selector:
    app: test-app
  ports:
    - name: http
      port: 80
      target_port: "8080"
      node_port: 30080
