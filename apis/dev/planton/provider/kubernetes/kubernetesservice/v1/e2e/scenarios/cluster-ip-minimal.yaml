apiVersion: kubernetes.planton.dev/v1
kind: KubernetesService
metadata:
  name: e2e-svc-clusterip
spec:
  namespace: default
  name: e2e-svc-clusterip
  selector:
    app: test-app
  ports:
    - name: http
      port: 80
      target_port: "8080"
