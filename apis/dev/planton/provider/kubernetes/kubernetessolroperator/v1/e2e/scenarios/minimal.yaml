apiVersion: kubernetes.planton.dev/v1
kind: KubernetesSolrOperator
metadata:
  name: e2e-slrop
spec:
  namespace:
    value: e2e-slrop
  create_namespace: true
  container:
    resources:
      limits:
        cpu: 500m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 64Mi
