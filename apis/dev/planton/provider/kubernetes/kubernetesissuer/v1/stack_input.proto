syntax = "proto3";

package dev.planton.provider.kubernetes.kubernetesissuer.v1;

import "dev/planton/provider/kubernetes/kubernetesissuer/v1/api.proto";
import "dev/planton/provider/kubernetes/provider.proto";

// Input for the KubernetesIssuer IaC stack.
message KubernetesIssuerStackInput {
  // Target cloud-resource.
  KubernetesIssuer target = 1;
  // Kubernetes provider configuration (cluster credentials).
  dev.planton.provider.kubernetes.KubernetesProviderConfig provider_config = 2;
}
