apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesissuer
spec:
  tier: 4
  status: skip
  deferred_reason: "requires cert-manager CRDs installed on the target cluster"
  validated_provisioners:
    - pulumi
  timeout_minutes: 10
