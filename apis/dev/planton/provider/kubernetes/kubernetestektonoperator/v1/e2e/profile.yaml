apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetestektonoperator
spec:
  tier: 4
  status: deferred
  deferred_reason: "Server-Side Apply field manager conflict on kind"
  validated_provisioners:
    - pulumi
  limitations:
    - "Operator auto-creates TektonConfig; Pulumi SSA conflict"
  timeout_minutes: 15
