apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesneo4j
spec:
  tier: 2
  status: deferred
  deferred_reason: "LoadBalancer service cannot get IP on kind"
  validated_provisioners:
    - pulumi
  timeout_minutes: 15
  limitations:
    - "requires cloud LB controller or MetalLB"
