syntax = "proto3";

package dev.planton.provider.kubernetes.kubernetesmanifest.v1;

import "dev/planton/provider/kubernetes/kubernetesmanifest/v1/api.proto";
import "dev/planton/provider/kubernetes/provider.proto";

// KubernetesManifestStackInput is the input for the Kubernetes manifest IaC module.
message KubernetesManifestStackInput {
  // target cloud-resource
  KubernetesManifest target = 1;
  // provider-config
  dev.planton.provider.kubernetes.KubernetesProviderConfig provider_config = 2;
}
