apiVersion: qa.planton.dev/v1
kind: ComponentE2EProfile
metadata:
  name: kubernetesistio
spec:
  tier: 4
  status: deferred
  deferred_reason: "gateway LoadBalancer timeout on kind"
  validated_provisioners:
    - pulumi
  limitations:
    - "Gateway LB stays Pending; base + istiod deploy fine"
  timeout_minutes: 15
