apiVersion: digital-ocean.planton.dev/v1
kind: DigitalOceanVolume
metadata:
  name: my-data-volume
spec:
  volumeName: my-data-volume
  region: nyc1
  sizeGib: 50
  filesystemType: ext4  # Pre-formatted, ready to mount immediately
  description: General-purpose block storage
  tags:
    - production
