syntax = "proto3";

package dev.planton.provider.oci.ocicontainerenginenodepool.v1;

import "dev/planton/provider/oci/ocicontainerenginenodepool/v1/api.proto";
import "dev/planton/provider/oci/provider.proto";

// OciContainerEngineNodePoolStackInput is the input to the IaC modules
// (Pulumi / Terraform) that deploy an OciContainerEngineNodePool resource.
message OciContainerEngineNodePoolStackInput {
  // target cloud-resource
  OciContainerEngineNodePool target = 1;
  // provider-config
  dev.planton.provider.oci.OciProviderConfig provider_config = 2;
}
