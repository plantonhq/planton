syntax = "proto3";

package dev.planton.provider.oci.ocikmsvault.v1;

import "dev/planton/provider/oci/ocikmsvault/v1/api.proto";
import "dev/planton/provider/oci/provider.proto";

// OciKmsVaultStackInput is the input to the IaC modules
// (Pulumi / Terraform) that deploy an OciKmsVault resource.
message OciKmsVaultStackInput {
  // target cloud-resource
  OciKmsVault target = 1;
  // provider-config
  dev.planton.provider.oci.OciProviderConfig provider_config = 2;
}
