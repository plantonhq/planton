# GcpVertexAiEndpoint Pulumi Module

This directory contains the Pulumi Go module for provisioning a GCP Vertex AI Endpoint.

## Quick Start

```bash
make build
pulumi preview --stack dev
pulumi up --stack dev --yes
```

## Debugging

```bash
./debug.sh
```

## Module Structure

See `overview.md` for architecture details.
