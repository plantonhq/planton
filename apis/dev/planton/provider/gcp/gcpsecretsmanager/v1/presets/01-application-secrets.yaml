apiVersion: gcp.planton.dev/v1
kind: GcpSecretsManager
metadata:
  name: my-app-secrets
spec:
  projectId:
    value: "<gcp-project-id>"
  secretNames:
    - database-password
    - api-key
    - oauth-client-secret
    - jwt-signing-key
