apiVersion: gcp.planton.dev/v1
kind: GcpArtifactRegistryRepo
metadata:
  name: my-npm-repo
spec:
  repoFormat: NPM
  projectId:
    value: "<gcp-project-id>"
  region: <gcp-region>
  enablePublicAccess: false  # private repository
