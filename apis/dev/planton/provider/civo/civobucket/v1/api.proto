syntax = "proto3";

package dev.planton.provider.civo.civobucket.v1;

import "buf/validate/validate.proto";
import "dev/planton/provider/civo/civobucket/v1/spec.proto";
import "dev/planton/provider/civo/civobucket/v1/stack_outputs.proto";
import "dev/planton/shared/metadata.proto";

// civo-bucket
message CivoBucket {
  //api-version
  string api_version = 1 [(buf.validate.field).string.const = 'civo.planton.dev/v1'];

  //resource-kind
  string kind = 2 [(buf.validate.field).string.const = 'CivoBucket'];

  //metadata
  dev.planton.shared.CloudResourceMetadata metadata = 3 [(buf.validate.field).required = true];

  //spec
  CivoBucketSpec spec = 4 [(buf.validate.field).required = true];

  //status
  CivoBucketStatus status = 5;
}

// civo-bucket status
message CivoBucketStatus {
  //stack-outputs
  //  civo-bucket stack-outputs
  CivoBucketStackOutputs outputs = 1;
}
