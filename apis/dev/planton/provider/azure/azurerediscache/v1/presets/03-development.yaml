apiVersion: azure.planton.dev/v1
kind: AzureRedisCache
metadata:
  name: my-dev-redis
spec:
  region: <azure-region>
  resourceGroup:
    value: "<your-resource-group-name>"
  name: <your-redis-name>
  skuName: Basic  # single node, no SLA (~$17/month for C0)
  capacity: 0  # C0: 250 MB cache size
  redisVersion: "6"  # default from spec
  nonSslPortEnabled: false  # default from spec
  minimumTlsVersion: "1.2"  # default from spec
  publicNetworkAccessEnabled: true  # default from spec
  maxmemoryPolicy: allkeys-lru  # pure cache: evict any key when full
