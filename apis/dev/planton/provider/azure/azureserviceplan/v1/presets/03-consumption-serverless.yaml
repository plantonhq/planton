apiVersion: azure.planton.dev/v1
kind: AzureServicePlan
metadata:
  name: my-consumption-plan
spec:
  region: <azure-region>
  resourceGroup:
    value: "<your-resource-group-name>"
  name: <plan-name>
  osType: Linux  # default from spec
  skuName: Y1  # Consumption plan — pay-per-execution
