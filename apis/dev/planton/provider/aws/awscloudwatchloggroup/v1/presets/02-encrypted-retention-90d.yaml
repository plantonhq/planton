apiVersion: aws.planton.dev/v1
kind: AwsCloudwatchLogGroup
metadata:
  name: ${name}
spec:
  retentionInDays: 90
  kmsKeyId:
    valueFrom:
      kind: AwsKmsKey
      name: ${kms_key_name}
      fieldPath: status.outputs.key_arn
