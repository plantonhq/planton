apiVersion: aws.planton.dev/v1
kind: AwsSnsTopic
metadata:
  name: my-fifo-topic
spec:
  region: <aws-region>
  fifoTopic: true
  contentBasedDeduplication: true
  fifoThroughputScope: MessageGroup
  signatureVersion: 2
