apiVersion: aws.planton.dev/v1
kind: AwsIamRole
metadata:
  name: my-lambda-execution-role
spec:
  region: <aws-region>
  description: Lambda function execution role with basic logging permissions
  path: /  # recommended_default from spec
  trustPolicy:
    Version: "2012-10-17"
    Statement:
      - Effect: Allow
        Principal:
          Service: lambda.amazonaws.com
        Action: sts:AssumeRole
  managedPolicyArns:
    # AWS-managed policies attach by literal ARN; reference an AwsIamPolicy's
    # policy_arn output via valueFrom for policies defined in Planton.
    - value: arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
