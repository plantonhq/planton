syntax = "proto3";

package dev.planton.provider.aws.awstransitgateway.v1;

import "dev/planton/provider/aws/awstransitgateway/v1/api.proto";
import "dev/planton/provider/aws/provider.proto";

// AwsTransitGatewayStackInput provides the inputs to the IaC modules
// (Pulumi or Terraform) that provision the Transit Gateway and its
// VPC attachments.
message AwsTransitGatewayStackInput {
  // target cloud-resource
  AwsTransitGateway target = 1;
  // provider-config
  dev.planton.provider.aws.AwsProviderConfig provider_config = 2;
}
