package module

const (
	// OpVolumeId is the exported stack output containing the volume UUID.
	OpVolumeId = "volume_id"
	// OpName is the exported stack output containing the volume name.
	OpName = "name"
	// OpSize is the exported stack output containing the volume size in GB.
	OpSize = "size"
	// OpVolumeType is the exported stack output containing the volume type.
	OpVolumeType = "volume_type"
	// OpAvailabilityZone is the exported stack output containing the AZ.
	OpAvailabilityZone = "availability_zone"
	// OpRegion is the exported stack output containing the OpenStack region.
	OpRegion = "region"
)
