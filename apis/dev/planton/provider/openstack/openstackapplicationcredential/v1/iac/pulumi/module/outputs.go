package module

const (
	// OpId is the exported stack output containing the credential UUID.
	OpId = "id"
	// OpName is the exported stack output containing the credential name.
	OpName = "name"
	// OpSecret is the exported stack output containing the credential secret (SENSITIVE).
	OpSecret = "secret"
	// OpProjectId is the exported stack output containing the project UUID.
	OpProjectId = "project_id"
	// OpRegion is the exported stack output containing the OpenStack region.
	OpRegion = "region"
)
