apiVersion: openstack.planton.dev/v1
kind: OpenStackNetworkPort
metadata:
  name: my-unrestricted-port
spec:
  networkId:
    value: <network-id>
  fixedIps:
    - subnetId:
        value: <subnet-id>
  noSecurityGroups: true
