syntax = "proto3";

package dev.planton.provider.openstack.openstackdnsrecord.v1;

import "dev/planton/provider/openstack/openstackdnsrecord/v1/api.proto";
import "dev/planton/provider/openstack/provider.proto";

// OpenStackDnsRecordStackInput is the input to the IaC module.
// It contains the target resource and provider configuration.
message OpenStackDnsRecordStackInput {
  // target cloud-resource to be deployed
  OpenStackDnsRecord target = 1;
  // provider-config for OpenStack authentication
  dev.planton.provider.openstack.OpenStackProviderConfig provider_config = 2;
}
