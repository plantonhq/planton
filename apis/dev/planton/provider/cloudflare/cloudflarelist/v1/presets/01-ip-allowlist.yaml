apiVersion: cloudflare.planton.dev/v1
kind: CloudflareList
metadata:
  name: ip-allowlist
spec:
  accountId: <cloudflare-account-id>
  kind: ip
  name: office_allowlist
  description: IPs allowed through WAF custom rules
