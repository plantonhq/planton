---
apiVersion: aws.planton.dev/v1
kind: AwsEcsCluster
metadata:
  name: "{{ values.env }}-ecs-cluster"
spec:
  region: "{{ values.aws_region }}"
  capacityProviders:
    - FARGATE
    - FARGATE_SPOT
