---
apiVersion: oci.planton.dev/v1
kind: OciVcn
metadata:
  name: "{{ values.env }}-vcn"
spec:
  compartmentId:
    value: "{{ values.compartment_ocid }}"
  cidrBlocks:
    - "{{ values.vcn_cidr }}"
  displayName: "{{ values.env }}-data-vcn"
  isNatGatewayEnabled: true
  isServiceGatewayEnabled: true
---
apiVersion: oci.planton.dev/v1
kind: OciSubnet
metadata:
  name: "{{ values.env }}-data-private"
spec:
  compartmentId:
    value: "{{ values.compartment_ocid }}"
  vcnId:
    valueFrom:
      kind: OciVcn
      name: "{{ values.env }}-vcn"
      fieldPath: status.outputs.vcnId
  cidrBlock: "{{ values.subnet_cidr }}"
  displayName: "{{ values.env }}-data-private"
  dnsLabel: data
  prohibitPublicIpOnVnic: true
  routeRules:
    - destination: "0.0.0.0/0"
      destinationType: cidr_block
      networkEntityId:
        valueFrom:
          kind: OciVcn
          name: "{{ values.env }}-vcn"
          fieldPath: status.outputs.natGatewayId
